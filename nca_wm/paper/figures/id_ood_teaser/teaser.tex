% ID/OOD teaser figure for the rule-conditioned NCA WM paper.
%
% Single-document standalone TikZ figure.
%
% Per game row: a "card pair" — a code Box flush against the WEST
% (left, reading-order back) face of the PuzzleScript state Box, both
% same height, both transparent. Code text on the shared east face;
% state image on the state's east face.
%
% Per section: a single skinny Encoder (one blue block) feeds a
% yellow Latent block ($z$). The latent block conditions every NCA
% application (drawn as just the 3 green NCA bands). ID rows use one
% NCA between s_0 and s_1; OOD rows repeat the NCA between every
% consecutive predicted frame so the autoregressive rollout is
% explicit.
%
% Latent scatters (large, above the figure) are connected from the
% yellow latent block by a dashed line — they're the 2D PCA view of
% the latent that the encoder produces.
%
\documentclass[border=4pt, multi, tikz]{standalone}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{import}
\subimport{../architecture/}{init}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
\usetikzlibrary{fit}
\usetikzlibrary{3d}
\usetikzlibrary{decorations.pathreplacing}

\colorlet{NCAColor}{green!35}
\colorlet{NCABandColor}{green!70}
\colorlet{EncColor}{blue!50}
\colorlet{SlotColor}{yellow!75}
\colorlet{PSFrameFill}{gray!12}
\colorlet{CodeFill}{white}

\colorlet{IDColor}{blue!55!black}
\colorlet{OODColor}{red!55!black}
\colorlet{LossColor}{orange!85!black}
\colorlet{CondColor}{violet!60!black}

% --- PS state frame (transparent). ---
\newcommand{\PSframe}[4]{%
  \pic[shift={(#2, #3, 0)}] at (0,0,0)
      {Box={name=#1, caption=, xlabel={{,}}, zlabel={},
            fill=PSFrameFill, opacity=0.35,
            height=4, width=1.0, depth=4}};%
  \node[canvas is zy plane at x={#2 + 0.2}] at (0, #3)
      {\includegraphics[width=0.8cm,height=0.8cm]{#4}};%
}

% --- Card pair: code box LEFT of state box, both transparent.
%     State image on state east face; code text on the shared boundary.
\newcommand{\StateOnCode}[5]{%
  \pic[shift={({#2 - 0.2}, #3, 0)}] at (0,0,0)
      {Box={name=#1c, caption=, xlabel={{,}}, zlabel={},
            fill=CodeFill, opacity=0.35,
            height=4, width=1.0, depth=4}};%
  \node[canvas is zy plane at x={#2}, font=\tiny\ttfamily,
        align=left, inner sep=0pt, text width=0.78cm,
        anchor=center]
        at (0, #3) {#5};%
  \pic[shift={(#2, #3, 0)}] at (0,0,0)
      {Box={name=#1g, caption=, xlabel={{,}}, zlabel={},
            fill=PSFrameFill, opacity=0.35,
            height=4, width=1.0, depth=4}};%
  \node[canvas is zy plane at x={#2 + 0.2}] at (0, #3)
      {\includegraphics[width=0.8cm,height=0.8cm]{#4}};%
}

% --- NCA "step": just the 3 green bands at (x, y). Anchors n#1-west / -east.
\newcommand{\NCAstep}[3]{%
  \pic[shift={(#2, #3, 0)}] at (0,0,0)
      {RightBandedBox={name=n#1, caption=, xlabel={{,,}}, zlabel={},
            fill=NCAColor, bandfill=NCABandColor,
            height=4, width={1.0,1.0,1.0}, depth=4}};%
}

% --- Encoder + Latent: one skinny blue Encoder + a yellow Latent ($z$)
%     block to its right. (#1=name, #2=enc x, #3=enc y).
\newcommand{\EncLatent}[3]{%
  \node[draw, rounded corners=1.5pt, fill=EncColor, line width=0.4pt,
        minimum width=0.30cm, minimum height=0.80cm, anchor=center]
        (enc_#1) at (#2, #3) {};
  \node[draw, rounded corners=1.5pt, fill=SlotColor, line width=0.4pt,
        minimum width=0.32cm, minimum height=0.80cm, anchor=west,
        font=\tiny] (slots_#1) at ({#2 + 0.45}, #3) {$z$};
  \draw[arrow, color=black!50] (enc_#1.east) -- (slots_#1.west);
}

\begin{document}

\begin{tikzpicture}
\tikzstyle{connection}=[thick, every node/.style={sloped,allow upside down},
    draw=\edgecolor, opacity=0.7]
\tikzstyle{arrow}=[-Stealth, line width=0.3mm, opacity=0.85]
\tikzstyle{lossarrow}=[-Stealth, line width=0.3mm, color=LossColor]
\tikzstyle{condarrow}=[-Stealth, line width=0.25mm, dashed,
    color=CondColor, opacity=0.75]
\tikzstyle{condline}=[line width=0.35mm, dashed,
    color=CondColor, opacity=0.85]

% =============================================================
%  LEFT HALF: in-distribution training
% =============================================================

\StateOnCode{neko}{-7}{ 1.5}{rollouts/nekopuzzle/engine_t0.png}
    {;nekopuzzle\\{}[>P|...|F]\\->[|...|P]}
\StateOnCode{soko}{-7}{ 0.0}{rollouts/sokoban_basic/engine_t0.png}
    {;sokoban\\{}[>P|C]\\->[>P|>C]}
\StateOnCode{mod} {-7}{-1.5}{rollouts/Modality/engine_t0.png}
    {;modality\\{}[>PW|B]\\->CANCEL}

% Encoder + latent block ABOVE the rows (y=2.4) so the state→NCA
% arrows don't pass through it.
\EncLatent{id}{-5.4}{2.4}

% One NCA per ID row.
\NCAstep{idtop}{-3}{ 1.5}
\NCAstep{idmid}{-3}{ 0.0}
\NCAstep{idbot}{-3}{-1.5}

\PSframe{s1_neko}{-1.5}{ 1.5}{rollouts/nekopuzzle/engine_t1.png}
\PSframe{s1_soko}{-1.5}{ 0.0}{rollouts/sokoban_basic/engine_t1.png}
\PSframe{s1_mod} {-1.5}{-1.5}{rollouts/Modality/engine_t1.png}

% State (blue) into NCA per row
\draw[arrow, color=IDColor] (nekog-east) -- (nidtop-west);
\draw[arrow, color=IDColor] (sokog-east) -- (nidmid-west);
\draw[arrow, color=IDColor] (modg-east)  -- (nidbot-west);
% NCA → s_1 per row
\draw[arrow, color=IDColor] (nidtop-east) -- (s1_neko-west);
\draw[arrow, color=IDColor] (nidmid-east) -- (s1_soko-west);
\draw[arrow, color=IDColor] (nidbot-east) -- (s1_mod-west);

% Code → encoder (dashed violet, fan-in upward to encoder above rows)
\draw[condarrow] (nekoc-east) to[out= 60,in=200] (enc_id.south);
\draw[condarrow] (sokoc-east) to[out= 60,in=215] (enc_id.south);
\draw[condarrow] (modc-east)  to[out= 60,in=230] (enc_id.south);

% Latent z conditions every ID NCA (down + over to each NCA top)
\draw[condarrow] (slots_id.south) to[out=-90,in= 90] (nidtop-north);
\draw[condarrow] (slots_id.south) to[out=-90,in= 90] (nidmid-north);
\draw[condarrow] (slots_id.south) to[out=-90,in= 90] (nidbot-north);

% Loss (dark orange): s_1 back to NCA per row
\draw[lossarrow] (s1_neko-west) to[out=180,in=  0] ($(nidtop-east)+(0,-0.10)$);
\draw[lossarrow] (s1_soko-west) to[out=180,in=  0] ($(nidmid-east)+(0,-0.10)$);
\draw[lossarrow] (s1_mod-west)  to[out=180,in=  0] ($(nidbot-east)+(0,-0.10)$);

% Latent scatter ABOVE the encoder (wider)
\node[anchor=south, inner sep=1pt] (latent_id) at (-4.0, 4.5)
    {\includegraphics[width=3.4cm]{latent_train_only.pdf}};
\node[font=\tiny\itshape, color=IDColor, anchor=south]
    at (-4.0, 7.15) {encoder latent space};
\draw[condline] (slots_id.north) -- (latent_id.south);

% ID banner
\node[font=\footnotesize\bfseries, color=IDColor, anchor=south, align=center]
    at (-4.0, 7.55) {Training (in-distribution)};

% =============================================================
%  VERTICAL DASHED BORDER between train and eval
% =============================================================
\draw[dashed, line width=0.3mm, color=black!70]
    (1.6, -2.4) -- (1.6,  7.8);
\node[font=\itshape\tiny, color=black!50, anchor=south, rotate=90]
    at (1.85, 0.4) {trained $\theta \rightarrow \theta^{*}$};

% =============================================================
%  RIGHT HALF: held-out eval — NCA repeated between every frame
% =============================================================

\StateOnCode{inway}{ 2.4}{ 1.5}
    {rollouts/in_the_way_by_giovanni_mota/pred_t0.png}
    {;in\_the\_way\\{}[>P|F]\\->[|P]}
\StateOnCode{nana}{ 2.4}{ 0.0}
    {rollouts/my_nana_ate_my_kid_brother_by_ruth_williams/pred_t0.png}
    {;my\_nana\\late[G|.|P]\\->[T|.|G]}
\StateOnCode{ham}{ 2.4}{-1.5}
    {rollouts/Hamiltwo/pred_t0.png}
    {;hamiltwo\\{}[>L|noW]\\->[>L+|noW]}

% Encoder + latent ABOVE the rows
\EncLatent{ood}{3.4}{2.4}

% NCA at x=4.7, ŝ_1 at 5.9, NCA at 7.1, ŝ_2 at 8.3, NCA at 9.5, ŝ_3 at 10.7
\NCAstep{ooinway1}{4.7}{ 1.5}
\NCAstep{ooinway2}{7.1}{ 1.5}
\NCAstep{ooinway3}{9.5}{ 1.5}
\NCAstep{oonana1}{4.7}{ 0.0}
\NCAstep{oonana2}{7.1}{ 0.0}
\NCAstep{oonana3}{9.5}{ 0.0}
\NCAstep{ooham1}{4.7}{-1.5}
\NCAstep{ooham2}{7.1}{-1.5}
\NCAstep{ooham3}{9.5}{-1.5}

\PSframe{h1_inway}{ 5.9}{ 1.5}{rollouts/in_the_way_by_giovanni_mota/pred_t1.png}
\PSframe{h2_inway}{ 8.3}{ 1.5}{rollouts/in_the_way_by_giovanni_mota/pred_t2.png}
\PSframe{h3_inway}{10.7}{ 1.5}{rollouts/in_the_way_by_giovanni_mota/pred_t3.png}

\PSframe{h1_nana}{ 5.9}{ 0.0}{rollouts/my_nana_ate_my_kid_brother_by_ruth_williams/pred_t1.png}
\PSframe{h2_nana}{ 8.3}{ 0.0}{rollouts/my_nana_ate_my_kid_brother_by_ruth_williams/pred_t2.png}
\PSframe{h3_nana}{10.7}{ 0.0}{rollouts/my_nana_ate_my_kid_brother_by_ruth_williams/pred_t3.png}

\PSframe{h1_ham}{ 5.9}{-1.5}{rollouts/Hamiltwo/pred_t1.png}
\PSframe{h2_ham}{ 8.3}{-1.5}{rollouts/Hamiltwo/pred_t2.png}
\PSframe{h3_ham}{10.7}{-1.5}{rollouts/Hamiltwo/pred_t3.png}

% State → NCA → ŝ chain per row
\draw[arrow, color=OODColor] (inwayg-east)    -- (nooinway1-west);
\draw[arrow, color=OODColor] (nooinway1-east) -- (h1_inway-west);
\draw[arrow, color=OODColor] (h1_inway-east)  -- (nooinway2-west);
\draw[arrow, color=OODColor] (nooinway2-east) -- (h2_inway-west);
\draw[arrow, color=OODColor] (h2_inway-east)  -- (nooinway3-west);
\draw[arrow, color=OODColor] (nooinway3-east) -- (h3_inway-west);

\draw[arrow, color=OODColor] (nanag-east)    -- (noonana1-west);
\draw[arrow, color=OODColor] (noonana1-east) -- (h1_nana-west);
\draw[arrow, color=OODColor] (h1_nana-east)  -- (noonana2-west);
\draw[arrow, color=OODColor] (noonana2-east) -- (h2_nana-west);
\draw[arrow, color=OODColor] (h2_nana-east)  -- (noonana3-west);
\draw[arrow, color=OODColor] (noonana3-east) -- (h3_nana-west);

\draw[arrow, color=OODColor] (hamg-east)    -- (nooham1-west);
\draw[arrow, color=OODColor] (nooham1-east) -- (h1_ham-west);
\draw[arrow, color=OODColor] (h1_ham-east)  -- (nooham2-west);
\draw[arrow, color=OODColor] (nooham2-east) -- (h2_ham-west);
\draw[arrow, color=OODColor] (h2_ham-east)  -- (nooham3-west);
\draw[arrow, color=OODColor] (nooham3-east) -- (h3_ham-west);

% Code → encoder fan-in (upward)
\draw[condarrow] (inwayc-east) to[out= 60,in=200] (enc_ood.south);
\draw[condarrow] (nanac-east)  to[out= 60,in=215] (enc_ood.south);
\draw[condarrow] (hamc-east)   to[out= 60,in=230] (enc_ood.south);

% Latent z conditions every NCA in every OOD row (it conditions the
% same θ* applied at each tick).
\foreach \name in {nooinway1, nooinway2, nooinway3,
                    noonana1, noonana2, noonana3,
                    nooham1, nooham2, nooham3} {%
  \draw[condarrow] (slots_ood.south) to[out=-90,in=90] (\name-north);
}

% Latent scatter ABOVE the encoder (wider)
\node[anchor=south, inner sep=1pt] (latent_ood) at (5.4, 4.5)
    {\includegraphics[width=3.4cm]{latent_with_ood.pdf}};
\node[font=\tiny\itshape, color=OODColor, anchor=south]
    at (5.4, 7.15) {encoder latent space + OOD ($\bigstar$)};
\draw[condline] (slots_ood.north) -- (latent_ood.south);

% OOD banner
\node[font=\footnotesize\bfseries, color=OODColor, anchor=south, align=center]
    at (5.4, 7.55) {Evaluation (held-out / OOD)};

\end{tikzpicture}

\end{document}
