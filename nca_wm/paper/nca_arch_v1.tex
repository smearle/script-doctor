
\documentclass[border=8pt, multi, tikz]{standalone}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{import}
\subimport{figures/architecture/}{init}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
\usetikzlibrary{fit}
\usetikzlibrary{3d}
\usetikzlibrary{decorations.pathreplacing}

\def\ConvColor{rgb:yellow,5;red,2.5;white,5}
\def\ConvReluColor{rgb:yellow,5;red,5;white,5}
\def\ConvSigmoidColor{rgb:blue,5;green,15}
\def\OnehotColor{rgb:white,5;blue,1}

\colorlet{EncColor}{blue!18}
\colorlet{EncBandColor}{blue!50}
\colorlet{SlotColor}{yellow!75}

\newcommand{\copymidarrow}{\tikz \draw[-Stealth,line width=0.8mm,draw={rgb:blue,4;red,1;green,1;black,3}] (-0.3,0) -- ++(0.3,0);}

\begin{document}
\begin{tikzpicture}
\tikzstyle{connection}=[ultra thick,every node/.style={sloped,allow upside down},draw=\edgecolor,opacity=0.7]
\tikzstyle{flowarrow}=[-Stealth, line width=0.5mm, opacity=0.85]
\tikzstyle{condarrow}=[-Stealth, line width=0.5mm, dashed,
    draw={rgb:blue,5;magenta,2;black,2}, opacity=0.9]
\tikzstyle{tflayer}=[draw, rounded corners=1.2pt, line width=0.3pt,
    minimum width=0.4cm, minimum height=2.4cm, inner sep=1pt]

% =============================================================
% LEFT: spec -> encoder -> slots (non-spatial)
% =============================================================

% --- input game spec ---
\node[draw, rounded corners=2pt, fill={gray!8},
      font=\footnotesize\ttfamily, align=left, inner sep=4pt,
      text width=3.6cm, anchor=east] (spec) at (-10.5,0)
{title sokoban\\
=========\\
objects\\
=========\\
\textbf{Player} \textbf{Crate}\\
\textbf{Wall} \textbf{Target}\\
=========\\
rules\\
=========\\
{[}\textbf{>} Player | Crate{]}\\
~->{[}|\textbf{>}P. \textbf{>}Cr.{]}};
\node[font=\small\itshape, anchor=south] at (spec.north) {game spec};

% --- Encoder block: 2 self-attn + 1 cross-attn ---
\node[tflayer, fill=EncColor]   (encS1) at (-8.8, 0) {};
\node[tflayer, fill=EncColor, right=0.08cm of encS1] (encS2) {};
\node[tflayer, fill=EncBandColor, right=0.08cm of encS2] (encX)  {};
\node[font=\scriptsize, rotate=90, inner sep=0pt] at (encS1) {self-attn};
\node[font=\scriptsize, rotate=90, inner sep=0pt] at (encS2) {self-attn};
\node[font=\scriptsize, rotate=90, inner sep=0pt, text=white!92!black]
    at (encX) {$K$-query x-attn};
\node[draw, rounded corners=2pt, line width=0.45pt, fit=(encS1)(encX),
      inner sep=3pt] (enc) {};
\node[font=\small\bfseries, anchor=south] at (enc.north)
    {Encoder $\mathrm{Enc}_\phi$};

% --- Slots ---
\node[draw, rounded corners=1.5pt, fill=SlotColor, line width=0.4pt,
      minimum width=1.1cm, minimum height=2.6cm, anchor=center]
      (slots) at (-6.4, 0) {};
\foreach \y in {-1.05,-0.85,-0.65,-0.45,-0.25,-0.05,0.15,0.35,0.55,0.75,0.95,1.15} {
  \draw[gray!60, line width=0.2pt]
      ($(slots.west)+(0.07, \y)$) -- ($(slots.east)+(-0.07, \y)$);
}
\node[font=\small\bfseries, anchor=south] at (slots.north) {slots $z$};

% --- spec -> enc -> slots ---
\draw[flowarrow] (spec.east) -- (enc.west);
\draw[flowarrow] (enc.east)  -- (slots.west);

% =============================================================
% RIGHT: dynamics row (s_t -> NCA body -> s_{t+1})
% Box height=40, scale=0.2  =>  vertical extent y in [-4, +4]
% =============================================================

% --- s_t (was 'embed' box, with state image projected on its face) ---
\pic[shift={(-3.5,0,0)}] at (0,0,0)
    {Box={
        name=embed,
        caption= ,
        xlabel={{9, }},
        zlabel=16,
        fill=\OnehotColor,
        height=40,
        width=2.5,
        depth=40
        }
    };
\node[canvas is zy plane at x=0] (sin) at (-3.5,0,0)
    {\includegraphics[width=3.6cm,height=3.6cm]{figures/architecture/state_in.png}};
\node[font=\large, anchor=north] at ($(embed-south)+(0,-0.4)$) {$s_t$};

% --- NCA body: a handful of layers (n_layers); whole body repeats n_repeats times ---
\pic[shift={(0,0,0)}] at (0,0,0)
    {RightBandedBox={
        name=ncal1,
        caption=3$\times$3---ReLu,
        xlabel={{32, }},
        zlabel=16,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=40,
        width=4,
        depth=40
        }
    };

\pic[shift={(2.6,0,0)}] at (0,0,0)
    {RightBandedBox={
        name=ncal2,
        caption=1$\times$1---ReLu,
        xlabel={{32, }},
        zlabel=16,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=40,
        width=4,
        depth=40
        }
    };

\pic[shift={(5.2,0,0)}] at (0,0,0)
    {RightBandedBox={
        name=ncal3,
        caption=1$\times$1---Sigmoid,
        xlabel={{9, }},
        zlabel=16,
        fill=\ConvColor,
        bandfill=\ConvSigmoidColor,
        height=40,
        width=2.5,
        depth=40
        }
    };

% Brace + label for n_layers (just above the body, label nudged right
% so it doesn't collide with the cross-attn arrow that descends onto ncal1).
\path let \p1 = (ncal1-north), \p2 = (ncal3-north) in
    coordinate (braceLeft)  at (\x1-0.20cm, \y1+0.10cm)
    coordinate (braceRight) at (\x2+0.20cm, \y2+0.10cm);
\draw[decorate, decoration={brace, amplitude=6pt, raise=4pt}, line width=0.4pt]
    (braceRight) -- (braceLeft);
\node[font=\small\itshape, anchor=south]
    at ($(braceLeft)!0.65!(braceRight)+(0,0.40cm)$) {$n_\text{layers}$};

% --- s_{t+1} (was 'readout' box, with state image projected on its face) ---
\pic[shift={(8.5,0,0)}] at (0,0,0)
    {Box={
        name=readout,
        caption= ,
        xlabel={{9, }},
        zlabel=16,
        fill=\OnehotColor,
        height=40,
        width=2.5,
        depth=40
        }
    };
\node[canvas is zy plane at x=0] (sout) at (8.5,0,0)
    {\includegraphics[width=3.6cm,height=3.6cm]{figures/architecture/state_out.png}};
\node[font=\large, anchor=north] at ($(readout-south)+(0,-0.4)$) {$s_{t+1}$};

% --- forward connections through dynamics row ---
\draw[connection] (embed-east)  -- node {\midarrow} (ncal1-west);
\draw[connection] (ncal1-east)  -- node {\midarrow} (ncal2-west);
\draw[connection] (ncal2-east)  -- node {\midarrow} (ncal3-west);
\draw[connection] (ncal3-east)  -- node {\midarrow} (readout-west);

% --- Loop arrow below the body (n_repeats), routed well below box bottoms.
%     Use \p1/\p2 to pin the descent to absolute screen-y values, since
%     adding plain offsets to pic-anchored 3D coordinates does not give
%     a clean vertical translation.
\path let \p1 = (ncal3-east), \p2 = (ncal1-west) in
    coordinate (loopOutR)  at (\x1+0.6cm, \y1)
    coordinate (loopOutRD) at (\x1+0.6cm, -7.5cm)
    coordinate (loopInLD)  at (\x2-0.6cm, -7.5cm)
    coordinate (loopInL)   at (\x2-0.6cm, \y2);
\draw[-Stealth, line width=0.9mm, opacity=0.32, rounded corners=16pt]
    (ncal3-east) -- (loopOutR) -- (loopOutRD) -- (loopInLD) -- (loopInL) -- (ncal1-west);
\node[font=\large\itshape, opacity=0.7]
    at ($(loopOutRD)!0.5!(loopInLD)+(0,0.55)$) {$\times\, n_\text{repeats}$};

% =============================================================
% Cross-attention: slots -> NCA body, routed above the figure.
% Land just left of ncal1's top edge so the descent does not pass
% through the n_layers brace label.
% =============================================================
\path let \p1 = (slots.north), \p2 = (ncal1-north) in
    coordinate (slotsTop)   at (\x1, \y1)
    coordinate (overslots)  at (\x1, 6.5cm)
    coordinate (overnca)    at (\x2-0.7cm, 6.5cm)
    coordinate (ncaTopLin)  at (\x2-0.7cm, \y2+0.10cm);

\draw[condarrow, rounded corners=10pt, line width=0.6mm]
    (slotsTop) -- (overslots) -- (overnca) -- (ncaTopLin);

\node[font=\footnotesize, text=blue!55!black, anchor=south, align=center]
    at ($(overslots)!0.5!(overnca)+(0,0.15cm)$)
    {keys, values \textit{(rule cross-attn, every step)}};

\end{tikzpicture}
\end{document}
