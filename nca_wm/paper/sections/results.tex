\section{Results}
\label{sec:results}

% User: This is true at the moment, but let's 
% We lead with our headline result---scaling rule diversity in a single
% rule-conditional NCA improves both in-distribution fidelity and
% transfer to unseen games (\S\ref{sec:results-scaling})---then work
% backwards through the architectural choices that make it possible.
% \S\ref{sec:results-multigame} fixes \textsc{Train-14} and asks which
% spatial body wins under multi-game training, motivating the choice of
% shared-weights NCA. \S\ref{sec:results-baselines} drills down to a
% single rule family (Sokoban) to expose the same architectural
% contrast in a memorization-free regime, and decomposes the NCA's pool
% stack and input-skip contributions.

\subsection{Scaling rule diversity: in-distribution and out-of-distribution}
\label{sec:results-scaling}
\label{sec:results-encoder-ablation}

A single rule-conditional NCA trained over a wider corpus must serve
two ends. In-distribution it should continue to fit the games it sees
even as the per-game share of capacity drops; out-of-distribution it
should transfer to unseen rule sets at evaluation time. We measure
both on the same checkpoints by training the canonical recipe
(shared-weights NCA, $n_h{=}256$, 150k updates) on
\textsc{Train-14} $\subset$ \textsc{Train-59} $\subset$
\textsc{Train-199} and evaluating each against (i) the 14 games
shared across all three presets, and (ii) \textsc{Heldout-26}, the
subset of the 30-game stratified held-out set. We isolate the encoder's contribution by
training a matched unconditional variant (no encoded slots) at every
scale, and report against the trivial identity baseline
$\hat s_{t+1}{=}s_t$, which lower-bounds the per-step state-change
rate. Conditional and unconditional models tie in-distribution at
\textsc{Train-14} (Table~\ref{tab:indist-scaling}: $1.63 / 0.11$
vs.\ $1.98 / 0.08$ random-AR mean / median; within $0.4$pp on every
regime), with the encoder's ID benefit emerging only at larger
corpora.

\begin{figure}[t]
  \centering
  \begin{subfigure}[b]{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/indist_intersection/intersection_slope_id.pdf}
    \caption{ID 1-step (TF).}
    \label{fig:intersection-slope-id}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/indist_intersection/intersection_slope_ood.pdf}
    \caption{OOD 1-step (TF).}
    \label{fig:intersection-slope-ood}
  \end{subfigure}
  \begin{subfigure}[b]{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/indist_intersection/intersection_slope_id_ar.pdf}
    \caption{ID random-action AR.}
    \label{fig:intersection-slope-id-ar}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/indist_intersection/intersection_slope_ood_ar.pdf}
    \caption{OOD random-action AR.}
    \label{fig:intersection-slope-ood-ar}
  \end{subfigure}
  \caption{\textbf{Cell-error as the training corpus grows from 14
    to 199 games}, on a numeric (log) corpus axis.
    \emph{In-distribution panels:} aggregate over the 14 games shared
    by every preset (the same game set evaluated against every
    checkpoint). In 1-step (TF), the unconditional model degrades $\approx 25\times$
    from \textsc{Train-14} to \textsc{Train-59}; the rule-conditional
    model degrades $\approx 5\times$ over the same range and barely
    moves to \textsc{Train-199}---rule-conditioning slows per-game
    dilution by an order of magnitude at fixed capacity. The AR panel
    shows the corresponding random-action rollout aggregate.
    \emph{Out-of-distribution panels:} aggregate on
    \textsc{Heldout-26} (per-cell totals broken out by regime in
    Appendix~\ref{app:cond-vs-uncond}, Table~\ref{tab:cond-vs-uncond-match});
    dotted black lines show
    the trivial identity baseline $\hat s_{t+1}{=}s_t$. Broadening
    the training corpus drops the conditional 1-step error
    $\approx 6\times$ from \textsc{Train-14} to \textsc{Train-199}
    ($14.8\% \to 2.3\%$ mean; $3.5\% \to 1.5\%$ median, reaching
    identity-level error). Both conditional and unconditional rows
    are reported across every scale; the conditional row's lead at
    \textsc{Train-14} erodes and partially reverses as the corpus
    grows---at \textsc{Train-199} the unconditional row has both the
    lower median and more wins vs.\ identity. Per-game cells and a
    stand-alone heatmap of the in-distribution intersection are in
    Appendix~\ref{app:dilution}.}
  \label{fig:intersection-slope}
\end{figure}

In-distribution (Figure~\ref{fig:intersection-slope-id}), both models
degrade on the shared 14 games as the corpus widens---data dilution at
fixed capacity---but rule-conditioning slows that dilution by roughly
an order of magnitude. Unconditional 1-step (TF) mean jumps from
$0.087\%$ (\textsc{Train-14}) to $2.20\%$ (\textsc{Train-59}), $\approx
25\times$, while conditional grows only $\approx 5\times$ over the
same range and is roughly flat to \textsc{Train-199} ($0.27\% \to
0.37\%$ mean; $0.021\% \to 0.157\%$ median). At \textsc{Train-199} the
unconditional baseline is $1.88\% / 0.46\%$, still $\approx 5\times$
the conditional mean and $\approx 3\times$ the median; per-game swings
can be dramatic (\textit{neko puzzle} goes from $0.000\%$ to $24.4\%$
when the encoder is removed). The encoder amortises shared dynamics
across games at fixed capacity.

Out-of-distribution (Figure~\ref{fig:intersection-slope-ood};
Appendix~\ref{app:cond-vs-uncond}, Table~\ref{tab:cond-vs-uncond-match}),
\textsc{Train-14} conditional fails on most heldout games (mean 1-step
(TF) $14.8\%$, $\approx 7\times$ identity's $2.23\%$); with $14\times$
more rule diversity, \textsc{Train-199} conditional drops to $2.30\% /
1.55\%$, matching identity's mean, beating its median, and winning on
$11/26$ heldout games at 1-step (TF). The encoder's clean OOD edge,
however, holds only at the smallest training scale: at \textsc{Train-14}
conditional beats unconditional ($14.8\% / 3.5\%$ vs.\ $19.6\% / 4.4\%$);
at \textsc{Train-59} the ordering already wobbles (uncond lower mean
$4.33\%$ vs.\ $5.73\%$, cond lower median $2.58\%$ vs.\ $2.95\%$ and
closer to identity on $20/26$ games); and at \textsc{Train-199}
unconditional has both the lower median and more wins vs.\ identity
($14$ vs.\ $11$). At broader corpora unconditional and conditional are
roughly tied on the held-out set; we return to what the unconditional
model is exploiting in \S\ref{sec:discussion}. On random-action AR
rollout, where errors compound over 30 steps, identity ($\approx 2\%$
mean) is a much stronger baseline: every model drifts to $7$--$27\%$ and
no current checkpoint beats identity over the full rollout on more than
a single game.

% Generalization-to-unseen-games results (held-out latent geometry,
% symbolic decoder reconstruction + sampling, frozen-NCA inverse fit
% from OOD transitions) live in Appendix~\ref{app:scaling-curve},
% \ref{app:autoencoder}, \ref{app:inverse-fit}.

\subsection{Architecture baselines}
\label{sec:results-baselines}
\label{sec:results-multigame}

The headline result fixes a single architecture---a shared-weights
NCA with $n_h{=}256$. We now ask whether that choice survives
head-to-head against stronger spatial bodies on three corpora that
probe different facets of the problem: \textsc{Train-14} (14 games,
$4{\times}4$ to $32{\times}24$ grids, rule counts 1--14) for
multi-game capacity sharing, single-game Sokoban on 10 authored
\textit{Microban} levels for the per-rule fitting limit (where slot
cross-attention can in principle memorize per-state outcomes), and
multi-grid synth Sokoban (widths $\{5,6,7,8\}^2$ $\times$ 256
evolved levels each; 1024 distinct levels share one rule set) where
that memorization crutch is removed. The per-step NCA uses
independent weights per iteration; CNN, U-Net, and ViT replace the
iterated local update with a non-iterative spatial body. All five
share the slot encoder and readout heads. Single-game runs train for
10k updates per seed, \textsc{Train-14} for 20k, $d_h=256$;
the per-step training objective does not separate architectures
(all five fit train cross-entropy within $\approx 2\%$ except ViT on
multi-grid synth, best loss $0.084 \pm 0.002$ vs.\
$0.069 \pm 0.003$ for the rest).

\begin{table}[t]
  \centering
  \caption{\textbf{Architecture baselines across three corpora.}
    BFS-AR cell-error (mean $\pm$ std over 3 seeds), per-cell,
    averaged across rollout steps and aggregated across games/levels;
    lower is better. Architectures sorted by \textsc{Train-14} BFS-AR
    ascending; per-column BFS winner and fewest parameters bolded.
    Params (M) are from the \textsc{Train-14} row; the per-preset
    drift and the random-AR / one-step (TF) columns are reported in
    Table~\ref{tab:baselines-arch-full} (Appendix~\ref{app:baselines-full}).}
  \label{tab:baselines-arch}
  \small
  \resizebox{\linewidth}{!}{%
  \input{figures/baselines/architectures_compact}%
  }
\end{table}

\paragraph{Multi-game \textsc{Train-14}.}
Both NCA variants outperform all three non-NCA baselines on
autoregressive rollout (U-Net at $1.8\times$, ViT at $2.3\times$
NCA-perstep's BFS-AR error); the NCA/non-NCA gap is robust to seed.
Within the NCA family the per-step body has the lowest BFS-AR error
($7.05\% \pm 0.87\%$) and one-step error ($0.25\% \pm 0.04\%$), while
the shared body has $5.8\times$ lower seed-to-seed variance and
$3.8\times$ better parameter efficiency; at $\pm 1\sigma$ the two
overlap on aggregate BFS-AR. ViT is again the only architecture
failing to fit the per-step objective (best loss $\approx 8 \times
10^{-4}$ vs.\ $\approx 0$), confirming the structural mismatch.
Per-game breakdowns: Appendix~\ref{app:per-game}.

\paragraph{Single-game Sokoban.}
The shared-weights NCA wins on BFS-AR cell-error
($0.003\% \pm 0.004\%$) at the fewest parameters (1.94M), an order
of magnitude below the same-depth feed-forward CNN
($0.055\% \pm 0.075\%$, $2.6\times$ more params) and roughly two
orders of magnitude below the per-step NCA ($0.283\% \pm 0.400\%$,
$3.8\times$ more params). U-Net's 2-level downsampling loses
fine-grained player position on small grids ($3.13\%$); ViT's global
self-attention fails to fit the data even on this single-game
preset ($9.32\%$ BFS-AR; one-step $1.49\%$ vs.\ $\approx 0$ for the
rest), confirming that unrestricted global attention does not match
grid-rule dynamics.

\paragraph{Multi-grid synthetic (memorization-free).}
With the per-state lookup removed, the shared-weights and per-step
NCAs are statistically tied ($2.08\% \pm 0.47\%$ vs.\
$2.09\% \pm 1.75\%$ BFS-AR), but the shared variant matches that
performance with $2.8\times$ fewer parameters; CNN trails both at
$2.82\% \pm 1.97\%$ despite roughly twice the depth. ViT fails to
fit the multi-grid synth data on the per-step training objective
itself (the only preset where any architecture leaves an
order-of-magnitude gap on train loss), and that gap shows up
downstream as a $\geq 4\times$ worse BFS-AR cell-error. Seed-to-seed
variance on this task is large ($\sigma > 1.5\%$ on BFS-AR for the
per-step NCA, CNN, and the input-skip-disabled ablation row) but the
gap to U-Net and ViT remains robust at $\pm 1\sigma$. Longer-form
architecture findings (per-game grid, hierarchical weight sharing)
are in Appendix~\ref{app:per-game-arch}.

\paragraph{Per-feature pool and input-skip ablation.}
We decompose the NCA-shared advantage by individually disabling each
component of the pool stack (axis max-pool, axis cumulative max,
global max) and the input-skip connection.

\begin{table}[t]
  \centering
  \caption{\textbf{Per-feature ablation of the NCA-shared recipe on
    multi-grid synth Sokoban.} Mean $\pm$ std over 3
    seeds at 10k updates. $\Delta$BFS reports the absolute increase
    in BFS-AR cell-error vs.\ the full recipe (relative figures in
    parentheses); both error columns are per-cell, averaged across
    rollout steps.}
  \label{tab:ablation-pool}
  \small
  \resizebox{\linewidth}{!}{%
  \input{figures/baselines/ablation_pool}%
  }
\end{table}

Percentages below are relative BFS-AR increases over the full recipe
(cf.\ $\Delta$BFS in Table~\ref{tab:ablation-pool}). Pool features
account for most of the non-local-context handling: dropping all
three ($+316\%$) costs roughly twice as much as the single most
important flavour, \emph{global-pool} ($+182\%$). \emph{Axis-pool} is
next ($+123\%$); \emph{axis-cummax}, designed for axis-aligned
``rule fires once X is in the same row/column as Y'' patterns,
contributes a smaller $+48\%$, consistent with the push rule
\texttt{[ > Player | Crate ] -> [ > Player | > Crate ]} depending
mostly on the $3{\times}3$ neighbourhood. We expect axis-cummax to
carry more weight on games like \textit{neko puzzle}, whose rule
\texttt{[ > Player | ... | Fruit ] -> [ | ... | Player ]} pushes a
fruit anywhere along the player's row; a multi-game version of this
ablation is left to future work.
\emph{Input-skip} raises BFS-AR by $+54\%$ when removed but
\emph{lowers} the one-step (TF) error ($0.168\%$ vs.\ $0.274\%$).
Re-injecting $(s_t, a_t)$ at every step anchors the body to the
input; without that anchor it fits engine transitions tighter at the
cost of robustness, and the resulting per-step errors compound when
the rollout feeds them back as off-manifold inputs.

% TODO: rerun per-feature pool ablation on Train-14 (multi-game) to
% verify global-pool dominance and the small axis-cummax contribution
% generalize beyond the single Sokoban rule.
