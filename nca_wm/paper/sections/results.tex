\section{Results}
\label{sec:results}

\subsection{Abstract-facing results still to add}
\label{sec:results-abstract-todos}

\papertodo{Headline dynamics fidelity table: for the full
human-authored corpus, report one-step exact state accuracy, changed
cell accuracy, 50/100/500-step rollout cell-error, first-divergence
step, and win-bit accuracy. Break down by train games, held-out
levels in seen games, and held-out games. This table should directly
support or revise the abstract's ``near-perfect fidelity over long
time horizons'' claim.}

\papertodo{Scaling curve: train on increasing numbers of games
(e.g. 1, 4, 14, 50, 100, all) with a fixed model family and report
rollout fidelity plus held-out-game performance. This is the missing
result for the final abstract sentence: ``By scaling data ... modeling
a game engine.''}

\papertodo{Symbolic autoencoder results: report reconstruction
accuracy, grammar-valid decode rate, engine-load success rate, and
examples of decoded games from training reconstructions and latent
samples. Include a table or figure showing that sampled latent games
are not merely malformed token sequences.}

\papertodo{Latent inverse-fitting results: freeze the NCA, optimize
an unknown game latent from OOD transition observations, decode it to
game code, and evaluate both transition fit and decoded-program
validity. Include at least one qualitative recovered-rule example and
a quantitative comparison to nearest-neighbor retrieval over training
game latents.}

\subsection{Baselines vs.\ rule-conditioned NCA}
\label{sec:results-baselines}

Table~\ref{tab:baselines-microban} reports the head-to-head comparison
on \textsc{microban} (10 authored levels), trained for 10k updates per
seed at $d_h = 256$ with the canonical recipe (\S\ref{sec:experiments}).
We measure best train cross-entropy loss, end-of-rollout cell-error
rate under three policies (random, BFS-optimal, A*-optimal), and final
parameter count. The table reports a single seed; the multi-seed
synthetic-data comparison is reported in
Table~\ref{tab:baselines-synth}.

\begin{table}[t]
  \centering
  \caption{\textbf{Baselines vs.\ rule-conditioned NCA on
    \textsc{microban} (authored levels).} Mean $\pm$ std over 3 seeds
    at 10k updates. Lower is better for loss and cell-error; A* first-
    divergence step is higher-is-better. Param count in millions;
    architectures sorted by BFS final cell-error.}
  \label{tab:baselines-microban}
  \small
  \begin{tabular}{l c c c c}
    \toprule
    Architecture & params (M) & best loss & BFS final-err & A* first-div \\
    \midrule
    \textbf{NCA (shared, $T{=}4, n_r{=}T$)} & \textbf{1.94} & $0.107 \pm 0.002$ & $\mathbf{0.11\% \pm 0.16\%}$ & $64$ \\
    CNN (4-block ResNet)                    & 5.00          & $0.107 \pm 0.002$ & $0.32\% \pm 0.24\%$         & $89$ \\
    NCA (per-step, $T{=}4, n_r{=}1$)        & 7.36          & $0.107 \pm 0.002$ & $0.47\% \pm 0.66\%$         & $35$ \\
    U-Net (2 levels)                        & 6.07          & $0.108 \pm 0.002$ & $4.84\% \pm 1.03\%$         & $14$ \\
    ViT (4 layers)                          & 3.99          & $0.107 \pm 0.002$ & $10.96\% \pm 0.50\%$        & $<1$ \\
    \bottomrule
  \end{tabular}
\end{table}

Three findings on this controlled, memorizable single-game setting:
\textbf{(i)} the NCA with shared weights wins on BFS rollout
($0.11\% \pm 0.16\%$) at the fewest parameters (1.94M),
$2.9\times$ better than the same-depth feed-forward CNN
($0.32\% \pm 0.24\%$) at $2.6\times$ fewer params and
$4.3\times$ better than the per-step NCA ($0.47\% \pm 0.66\%$);
\textbf{(ii)} U-Net and ViT both actively underperform here despite
parameter counts comparable to or exceeding NCA-shared --- U-Net's
2-level downsampling loses fine-grained player position on small
grids, and ViT's global self-attention diverges before the first
rollout step (mean first-divergence step under $1$), suggesting
locality plus iteration is a stronger inductive bias for grid-rule
dynamics than unrestricted global attention; \textbf{(iii)} best
train loss is essentially identical ($0.107 \pm 0.002$) across all
five architectures, illustrating finding F3
(\S\ref{sec:results-F3}): train loss is invisible to a $\geq 100\times$
swing in autoregressive rollout error ($0.11\%$ vs.\ $10.96\%$).

\medskip\noindent
The full comparison on the multi-grid synthetic data
regime---where memorization is unavailable and rule learning is
required---is reported in Table~\ref{tab:baselines-synth} and
analyzed in \S\ref{sec:results-ceiling}.

\begin{table}[t]
  \centering
  \caption{\textbf{Baselines vs.\ rule-conditioned NCA on
    \textsc{sokoban\_basic} (multi-grid synthetic).} Mean $\pm$ std
    over 3 seeds at 10k updates; widths
    $\{5{\times}5, 6{\times}6, 7{\times}7, 8{\times}8\}$ with 256
    synthetic levels per width. Memorization is unavailable; the model
    has to learn the slide rule across grid sizes. Architectures
    sorted by BFS final cell-error.}
  \label{tab:baselines-synth}
  \small
  \begin{tabular}{l c c c c}
    \toprule
    Architecture & params (M) & best loss & BFS final-err & A* first-div \\
    \midrule
    \textbf{NCA (shared, $T{=}4, n_r{=}T$)} & \textbf{3.07} & $0.069 \pm 0.003$ & $\mathbf{4.37\% \pm 2.02\%}$ & $9.2$ \\
    CNN (4-block ResNet)                    & 5.00          & $0.069 \pm 0.003$ & $5.56\% \pm 3.12\%$         & $7.0$ \\
    NCA (per-step, $T{=}4, n_r{=}1$)        & 8.49          & $0.070 \pm 0.003$ & $5.95\% \pm 3.50\%$         & $13.8$ \\
    U-Net (2 levels)                        & 6.07          & $0.071 \pm 0.003$ & $8.33\% \pm 0.97\%$         & $0.5$ \\
    ViT (4 layers)                          & 3.99          & $0.084 \pm 0.002$ & $12.70\% \pm 1.12\%$        & $0.7$ \\
    \bottomrule
  \end{tabular}
\end{table}

The synthetic-multi-grid setting is the headline comparison: per-state
memorization is unavailable (4 widths $\times$ 256 synthetic levels
per width $=$ 1024 distinct levels share one rule set), so the model
must \emph{learn the rule that maps state plus action to the next
state}. Three observations: \textbf{(i)} the NCA with shared weights
again wins on BFS rollout ($4.37\% \pm 2.02\%$) at the fewest
parameters (3.07M), beating the matched-depth CNN
($5.56\% \pm 3.12\%$) and the per-step NCA
($5.95\% \pm 3.50\%$); the $2.8\times$ parameter advantage over
NCA-per-step suggests the shared-weights inductive bias is doing real
work, not just compressing capacity; \textbf{(ii)} U-Net
($8.33\% \pm 0.97\%$) and ViT ($12.70\% \pm 1.12\%$) underperform
here as well, with ViT also failing to fit the train loss
($0.084 \pm 0.002$ vs.\ $0.069 \pm 0.003$ for the other four);
\textbf{(iii)} the seed-to-seed variance on this task is large
($\sigma > 2\%$ on BFS error for all NCA / CNN variants), reflecting
the multiple-basin optimization landscape we previously characterized
on the closely related \textsc{varislide} task (F8); the large gap
to U-Net and ViT is robust to that variance.

\paragraph{(F1) Pool features are load-bearing.}
A bare $3{\times}3$ convolution can only propagate one cell of context
per NCA step. Several PuzzleScript rule patterns reference cells
arbitrarily far apart along an axis or anywhere on the level
(\texttt{[X | ... | Y]}, \texttt{[X][Y]}). Without pool features the
NCA needs $\Omega(\max(H,W))$ steps just to surface non-local
information; with axis-cummax + global pool one step suffices.
Across the 19-game \textsc{scaling\_large} preset and a controlled
\textsc{collapse} no-pool ablation, turning pool off costs both
convergence speed and final loss, with the no-pool model collapsing
into the identity-prediction minimum even at $4{\times}$ the depth.

\paragraph{(F2) Shared NCA-body weights are the right inductive bias.}
The PuzzleScript engine has two natural levels of iteration: an
inner ordered list of rules applied once each per tick, and an outer
\texttt{again} loop that re-runs the rule list until the state stops
changing. We factor $T = n_\ell \cdot n_r$ accordingly. On a single-game
depth sweep ($T \in \{2, 4, 8, 16\}$), shared $T{=}8$ reaches
$5.5{\times}$ lower train loss with $7{\times}$ fewer parameters than
the per-step body at the same depth; shared $T{=}2$ at 14$\times$
fewer parameters than the deepest per-step variant matches it on
oracle-action rollouts. Hierarchical $n_r > 1$ sits between these
extremes and is the subject of an ongoing ablation
(\S\ref{sec:results-hierarchy}).

\paragraph{(F3) Train loss decouples from autoregressive rollout error.}
\label{sec:results-F3}
A model with the lowest train (per-step) loss is not the model with
the lowest autoregressive rollout error. Concretely, in the
over-capacity regime we observe per-step models with
$5{\times}$ lower train loss but $3{-}4{\times}$ higher 50-step
rollout error. The mechanism is small but systematic per-step errors
compounding destructively under autoregression. Reporting only
best-loss hides the rollout-drift cost of going deeper or wider.

\subsection{Pool-feature and input-skip ablation}
\label{sec:results-pool}

We decompose the NCA-shared advantage on the multi-grid synth setting
(Table~\ref{tab:baselines-synth}) by individually disabling each
component of the pool stack and the input-skip connection. Every
NCA step in the full recipe sees three flavours of broadcast
non-local features --- axis max-pool (row + column max), axis
cumulative max (directional prefix-max in each of four directions),
and grid-wide global max --- plus a re-injection of the embedded
$(s_t, a_t)$ at every step. Results in Table~\ref{tab:ablation-pool}.

\begin{table}[t]
  \centering
  \caption{\textbf{Per-feature ablation of the NCA-shared recipe on
    multi-grid synth \textsc{sokoban\_basic}.} Mean $\pm$ std over 3
    seeds at 10k updates. $\Delta$BFS reports the absolute increase
    in BFS final cell-error vs.\ the full recipe; relative figures
    in parentheses. Train loss is essentially constant across all
    configurations (F3 still applies).}
  \label{tab:ablation-pool}
  \small
  \begin{tabular}{l c c c c}
    \toprule
    Recipe & params (M) & best loss & BFS final-err & $\Delta$BFS vs.\ full \\
    \midrule
    \textbf{Full (axis + cummax + global + input-skip)} & 3.07 & $0.098 \pm 0.004$ & $\mathbf{4.37\% \pm 2.02\%}$ & --- \\
    \midrule
    no axis-cummax    & 2.94 & $0.098 \pm 0.003$ & $4.37\% \pm 2.24\%$  & $+0.00$ ($+0\%$) \\
    no input-skip     & 2.48 & $0.098 \pm 0.004$ & $5.95\% \pm 4.24\%$  & $+1.58$ ($+36\%$) \\
    no axis-pool      & 2.94 & $0.100 \pm 0.004$ & $6.75\% \pm 0.56\%$  & $+2.38$ ($+55\%$) \\
    no global-pool    & 3.01 & $0.098 \pm 0.005$ & $10.71\% \pm 2.57\%$ & $+6.35$ ($+145\%$) \\
    no pool at all    & 2.68 & $0.100 \pm 0.005$ & $10.71\% \pm 3.89\%$ & $+6.35$ ($+145\%$) \\
    \bottomrule
  \end{tabular}
\end{table}

Three observations:
\textbf{(i)} \emph{global-pool} is the single most important pool
feature. Removing it alone is as bad as removing all three pool
flavours combined ($+145\%$ relative BFS error in both cases),
indicating that grid-wide presence-of-X signals are doing all the
non-local work on this task; \textbf{(ii)} \emph{axis-cummax} ---
the directional prefix-max designed for axis-aligned ``rule fires
once X is in the same row/column as Y'' patterns --- contributes
nothing measurable on \textsc{sokoban\_basic} ($+0\%$). Sokoban
chained-pushing rules look like
\texttt{[ Crate | Crate ] -> [\,| Crate Crate\,]}, which depends
on the immediate $3{\times}3$ neighbourhood, not a directional axial
lookup. We expect the cummax feature to recover its load-bearing
role on games whose rule sets do reference far-apart same-axis cells
(e.g.\ \textsc{collapse}, \textsc{varislide});
\textbf{(iii)} \emph{input-skip} contributes a modest $+36\%$ but
with the highest variance across configurations
($\sigma = 4.24\%$), suggesting it is a stability mechanism rather
than an information-carrying feature: when training lands in a good
basin it isn't strictly needed, but it cuts the bad-basin tail.
Train loss is essentially identical ($0.098 \pm 0.004$) across all
six configurations despite a $2.5\times$ swing in BFS rollout
error --- yet another instance of F3 (\S\ref{sec:results-F3}).

\subsection{Hierarchical weight sharing}
\label{sec:results-hierarchy}

[Figure: $T{=}16$ varying $n_r \in \{1, 2, 4, 8, 16\}$ on
\textsc{microban} multi-grid synth and on the multi-game
\textsc{scaling\_14} preset. Reports change-error and parameter count.]

\subsection{Adaptive halting}
\label{sec:results-halting}

We compare four halting modes and a fixed-depth baseline on a custom
$1$-rule \textsc{varislide} game in which a single \texttt{again}-driven
slide rule pushes the player up to $d \in \{1, 2, 3, 4, 6, 8, 12, 16\}$
cells per action across grid widths $\{6, 8, 10, 12, 16\}$. Modes:
\emph{ponder} (PonderNet ponder loss with learned halt head + KL
prior), \emph{uniform} (per-step loss equally weighted across all
$T$ steps), \emph{argmax-ST} (straight-through estimator on
$\arg\max_k p_k$), and \emph{convergence-ST} (halt at inference when
consecutive predictions stop changing, no learned head).

[Figure: per-step error trajectory under each mode + the per-instance
halt distribution as a function of slide distance.]

Across these modes we observe two phenomena. First, on tasks where
the dynamics fit cleanly in $\sim$1--2 NCA steps (small-grid varislide,
\textsc{collapse}), all halting modes converge near the dynamics-floor
within their effective depth and drift past it; convergence-ST
recovers near-oracle performance via inference-time stopping.
Second, on the multi-grid synthetic regime where memorization is
unavailable, \emph{none} of the halting modes reliably learn the
underlying rule (\S\ref{sec:results-ceiling}); the halt head at best
degenerates to halt-at-$k{=}1$ + identity prediction.

\subsection{Multi-grid synthetic data ceiling}
\label{sec:results-ceiling}

When we move from a single authored level to the multi-grid synthetic
data regime ($\{6,8,10,12,16\}$ widths $\times$ 64 levels each),
train change-error climbs by $1000{\times}$ ($5 \times 10^{-5}
\to 0.05$--$0.10$), confirming that the model can no longer
1-shot-memorize per-state outcomes. The model attempts to learn the
rule but plateaus: 50k updates at $d_h{=}256$ (5$\times$ more updates,
2$\times$ wider) does not move the needle. Multi-seed verification at
$T \in \{8, 16, 32, 64\}$ shows per-seed argmax variance dwarfs the
recipe signal and best-loss is essentially identical across all
configurations. We discuss candidate fixes in \S\ref{sec:discussion}.

\subsection{Multi-game scaling}
\label{sec:results-scaling}

Table~\ref{tab:baselines-scaling14} reports the head-to-head comparison
on the \textsc{scaling\_14} benchmark: 14 PuzzleScript games sharing
the rule-slot encoder, with grid sizes from $4{\times}4$ to
$32{\times}24$ and rule counts from a handful (basic Sokoban) to
$\sim$30 (\texttt{Heroes\_of\_Sokoban}, \texttt{Sokoboros}). Single
seed, 20k updates, $d_h = 256$.

\begin{table}[t]
  \centering
  \caption{\textbf{Multi-game baselines on \textsc{scaling\_14}.}
    Mean $\pm$ std over 3 seeds at 20k updates. Cell-error rate
    averaged across games and levels. Both NCA variants beat all
    three non-NCA baselines; within the NCA family, the per-step
    body wins on BFS rollout while the shared body wins on train loss
    (parameter-efficient) and has notably lower seed-to-seed variance.}
  \label{tab:baselines-scaling14}
  \small
  \begin{tabular}{l c c c c}
    \toprule
    Architecture & params (M) & best loss & BFS final-err & A* final-err \\
    \midrule
    \textbf{NCA (per-step, $T{=}4, n_r{=}1$)} & 7.37 & $\sim$$0$ & $\mathbf{12.39\% \pm 1.82\%}$ & $13.57\% \pm 1.76\%$ \\
    NCA (shared, $T{=}4, n_r{=}T$)            & \textbf{1.96} & $\sim$$0$ & $13.12\% \pm \mathbf{0.24\%}$ & $14.68\% \pm 0.08\%$ \\
    CNN (4-block ResNet)                      & 5.01 & $\sim$$0$ & $13.56\% \pm 1.08\%$ & $14.71\% \pm 1.42\%$ \\
    U-Net (2 levels)                          & 6.09 & $1.0 \times 10^{-4}$ & $21.03\% \pm 0.29\%$ & $23.15\% \pm 0.40\%$ \\
    ViT (4 layers)                            & 4.01 & $7.7 \times 10^{-4}$ & $23.14\% \pm 2.40\%$ & $24.67\% \pm 1.87\%$ \\
    \bottomrule
  \end{tabular}
\end{table}

The multi-game story is more nuanced than the single-game one.
\textbf{(i)} Both NCA variants outperform all three non-NCA baselines
on autoregressive rollout, with U-Net at $1.6\times$ and ViT at
$\sim$$1.9\times$ NCA-perstep's BFS error; the NCA / non-NCA gap is
robust to seed (no overlap of $\pm 1\sigma$ intervals).
\textbf{(ii)} Within the NCA family, the per-step body has the
lowest mean BFS error ($12.39\% \pm 1.82\%$) but the shared body
has notably lower seed-to-seed variance ($\pm 0.24\%$ vs.\
$\pm 1.82\%$); the per-step's higher capacity helps the model encode
the joint distribution of 14 distinct rule sets, while the shared
body's compactness yields a more constrained optimization landscape.
At $\pm 1\sigma$ NCA-shared and NCA-perstep overlap, so we cannot
distinguish them on aggregate BFS rollout, but NCA-shared wins
parameter efficiency by $3.8\times$.
\textbf{(iii)} ViT's train loss is $\sim$$10^{4}$ times higher than
the NCA / CNN family ($\sim$$8 \times 10^{-4}$ vs.\ $\sim$$0$),
confirming the structural mismatch we observed on the synthetic
single-game setting (\S\ref{sec:results-baselines}): unrestricted
global attention does not fit grid-rule dynamics well even at modest
scale. \textbf{(iv)} Per-game breakdowns and held-out cross-game
generalization are deferred to the appendix.
