\section{Implementation details}
\label{app:impl}

\subsection{Model construction}

All models are implemented in JAX/Flax with the Adam optimizer. The
slot encoder is identical across architectures (see
\S\ref{sec:method}). The hidden grid dimension $d_h$, the cell
update structure, and the readout heads are also shared; only the
spatial-update body (rule-attention NCA, CNN, U-Net, or ViT) varies.

\subsection{Tokenization}

Each game's tokenized representation is a sequence of integers from
a $\approx 140$-symbol vocabulary covering: object names, layer
references, rule operators, action types, the 5$\times$5 sprite RGBA
values when sprite tokens are enabled, and per-rule kernel
separators. A single token sequence per game serves as the encoder
input.

\subsection{Dataset provenance and training suites}
\label{app:data-provenance}

After filename and token-hash deduplication, all training and held-out
splits are defined by explicit game-name lists.

\paragraph{Single-game Sokoban level provenance.}
The 10 authored Sokoban levels used for the single-game evaluation
(Table~\ref{tab:baselines-sokoban}) are drawn from the Microban level
pack, a community-authored set of Sokoban puzzles. Microban shares
its rule set 1-to-1 with PuzzleScript's basic Sokoban game
(\textsc{sokoban\_basic}); the only thing it adds is additional
authored levels for the same dynamics. Wherever this appendix
references \textsc{Microban} or \textsc{sokoban\_basic} explicitly
(e.g.\ in the per-game $\times$ per-architecture grid in
\S\ref{app:per-game-arch}), it refers to this same Sokoban rule set.

\paragraph{\textsc{Train-14} (14 games).}
\textsc{nekopuzzle}, \textsc{notsnake}, \textsc{blocks},
\textsc{sokoban\_basic}, \textsc{sokoban\_match3},
\textsc{Zen\_Puzzle\_Garden}, \textsc{Multi-word\_Dictionary\_Game},
\textsc{kettle}, \textsc{Travelling\_salesman},
\textsc{Collapsable\_Sokoban}, \textsc{Love\_and\_Pieces},
\textsc{actiontest}, \textsc{scriptcross}, \textsc{Modality}.

\paragraph{\textsc{Train-94} (94 games).}
The \textsc{Train-94} preset is the deduplicated 57-game gallery
suite (filter: $\le 20$ rules, $\le 20$ objects, $\le 30$ max level
area, $\le 30$ levels) plus 37 stratified additions drawn from the
3{,}474-game dedup pool (12 low, 15 mid, 8 high, 2 xhigh rule-count
buckets).

\textit{Deduplicated 57-game gallery base:}
{\small
\textsc{nekopuzzle}, \textsc{notsnake}, \textsc{blocks},
\textsc{sokoban\_basic}, \textsc{sokoban\_match3},
\textsc{Zen\_Puzzle\_Garden}, \textsc{Multi-word\_Dictionary\_Game},
\textsc{kettle}, \textsc{Travelling\_salesman}, \textsc{sumo},
\textsc{the\_undertaking}, \textsc{wrappingrecipe},
\textsc{Collapsable\_Sokoban}, \textsc{Love\_and\_Pieces},
\textsc{actiontest}, \textsc{rigidfail1}, \textsc{scriptcross},
\textsc{Modality}, \textsc{constellationz}, \textsc{randomrobots},
\textsc{againexample}, \textsc{naughtysprite}, \textsc{randomspawner},
\textsc{twolittlecrates1}, \textsc{rigid\_11},
\textsc{Long\_Haul\_Space\_Flight}, \textsc{leftrightnpcs},
\textsc{twolittlecrates2}, \textsc{twolittlecrates3}, \textsc{octat},
\textsc{lunar\_lockout}, \textsc{Stairways},
\textsc{the\_art\_of\_cloning}, \textsc{rigid\_scott1},
\textsc{rigid\_one\_unlimited},
\textsc{Some\_lines\_were\_meant\_to\_be\_crossed},
\textsc{blockfaker}, \textsc{Pushing\_It}, \textsc{2D\_Whale\_World},
\textsc{MazezaM}, \textsc{Ebony\_\&\_Ivory},
\textsc{Singleton\_Traffic}, \textsc{mazetest}, \textsc{Slidings},
\textsc{Lime\_Rick}, \textsc{riverpuzzle}, \textsc{Midas},
\textsc{rigid\_parallel\_many}, \textsc{Take\_Heart\_Lass},
\textsc{rigid\_many\_broken}, \textsc{Lightdown},
\textsc{The\_observer's\_paradox},
\textsc{rigid\_parallel\_unlimited},
\textsc{MC\_Escher's\_Equestrian\_Armageddon}, \textsc{Smother},
\textsc{It\_Dies\_In\_The\_Light}, \textsc{Pushcat\_Jr}.
}

\textit{Low-rule additions (12):}
{\small
\textsc{Time-reversed\_Microban},
\textsc{clone\_cloned\_clones!\_by\_sammytooch},
\textsc{bell,\_book,\_and\_candle\_by\_bagenzo},
\textsc{zombie\_bedtime\_by\_dan}, \textsc{Dot\_Puzzle\_3.1},
\textsc{cat\_in\_a\_box\_by\_trevor},
\textsc{no\_name\_yet\_by\_dustin\_lane},
\textsc{bit\_treats\_by\_barefootengineer1},
\textsc{Carnival\_Shooter!},
\textsc{sokobee\_simples\_by\_matteomenapace},
\textsc{shadow\_by\_unknown\_author},
\textsc{my\_game\_by\_calvin\_mcnamara}.
}

\textit{Mid-rule additions (15):}
{\small
\textsc{neko\_puzzle\_by\_increpare},
\textsc{division\_by\_paul\_gomez},
\textsc{el\_laberinto\_dorado\_by\_skyemoure},
\textsc{simple\_block\_pushing\_game\_by\_maximyzer},
\textsc{snowman\_game\_by\_yuna},
\textsc{the\_adventurer\_by\_mose\_e.eckman},
\textsc{boxob\_by\_randomnamemn},
\textsc{\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_by\_bagenzo},
\textsc{pokemon\_gooooo\_by\_drewdrewsan},
\textsc{simple\_foxes\_by\_torybash},
\textsc{run\_em\_over\_by\_xxsethyxxxoultraomeg},
\textsc{encuentra\_a\_tu\_amigo\_by\_tobiasguzzo},
\textsc{build\_a\_snowman\_v2.0\_by\_arthur\_yi},
\textsc{constellation\_by\_jere\_majava},
\textsc{avaruus\_tunkeutujat\_by\_janjulije}.
}

\textit{High-rule additions (8):}
{\small
\textsc{Union\_Move}, \textsc{cyber\_sokoban\_by\_eraykaan},
\textsc{game\_by\_jobiden123},
\textsc{nabokos\_3\_-\_tech\_demo\_by\_asynartesies},
\textsc{buny\_hoping\_by\_pickten},
\textsc{drunkard\_walk\_by\_unknown\_author},
\textsc{directional\_insanity\_by\_zithral},
\textsc{tron\_by\_lorapel}.
}

\textit{Xhigh-rule additions (2):}
{\small
\textsc{venganza\_i\_by\_felipe\_gomez},
\textsc{spell\_by\_beekie18}.
}

\paragraph{\textsc{Train-199} (199 games).}
\textsc{Train-199} is the same 57-game gallery base as
\textsc{Train-94} extended with 142 additional stratified games drawn
from the dedup pool under the same filter. The full list is given
below at \texttt{\textbackslash footnotesize}.

\textit{57-game gallery base:} as in \textsc{Train-94} above.

\textit{142 stratified additions:}
{\footnotesize
\textsc{bit\_treat\_by\_barefootengineer1}, \textsc{error\_1\_by\_bvoq},
\textsc{threat\_level\_midnight\_by\_vignesh},
\textsc{the\_magic\_broom\_by\_radiosoap},
\textsc{davib\_by\_thebigfive-381},
\textsc{cat\_adventure\_deluxe!\_demo\_by\_testethetestcat},
\textsc{Opposition}, \textsc{my\_eyes!!!\_by\_explodedcoder},
\textsc{a\_game\_by\_password69},
\textsc{bombero\_en\_acción\_by\_tevo66},
\textsc{displacement\_by\_oliverwebsim},
\textsc{bittle\_bat\_by\_unknown\_author},
\textsc{match\_threekoban\_by\_ezra\_szanton},
\textsc{my\_game\_by\_amanda\_howell},
\textsc{puzzle\_by\_unknown\_author},
\textsc{super\_mario\_nazi\_killer\_by\_koldo},
\textsc{ditto\_by\_acercandrito},
\textsc{the\_far\_away\_danish\_pastry\_is\_always\_deliciouser\_by\_increpare\_[variant\_of\_lexaloffles\_neko\_puzzle]},
\textsc{prototype\_1\_by\_caurso99},
\textsc{herding\_cats!!!1!\_by\_docchewkie},
\textsc{robot\_run\_by\_veeraalt},
\textsc{sokobad\_by\_noacubestudio},
\textsc{block\_pushing\_game\_tutorial\_2-4\_by\_unknown\_author},
\textsc{2to\_by\_yeah-and-no}, \textsc{blockpush\_by\_tapcat},
\textsc{simple\_block\_pushing\_game\_by\_zaydiscool777},
\textsc{neko\_puzzle\_by\_increpare},
\textsc{no\_forbidden\_symbols\_by\_increpare},
\textsc{All\_These\_Damn\_Crates},
\textsc{bahçıvanın\_çiçekleri\_by\_harunylc},
\textsc{t-rex\_hot\_chocolate\_by\_therealeddawson},
\textsc{zombie\_blocks\_by\_aidan\_quade},
\textsc{the\_poop\_game\_by\_pontus\_granstrom},
\textsc{hanoi\_by\_sfiera},
\textsc{feed\_the\_flames\_by\_callum\_scott},
\textsc{match\_by\_0creds},
\textsc{Monster\_block\_push\_game},
\textsc{welcome\_to\_flatland\_by\_caitlin\_weaver},
\textsc{switch\_switcher\_by\_increpare},
\textsc{snow\_day\_by\_forgdeer},
\textsc{ambiguity\_generalization\_prototype\_by\_thebigfive-381},
\textsc{avain\_peli\_by\_my\_name\_here},
\textsc{midnight\_snack\_by\_lilianaaaaaaaa},
\textsc{de+a+ch\_by\_razthepenguin},
\textsc{variations\_on\_constellation\_z\_by\_n\_a},
\textsc{rabbit\_meets\_camel\_by\_james\_wood},
\textsc{golem\_five\_by\_henketime},
\textsc{neko\_puzzle\_by\_necrothaum},
\textsc{puzzle\_script\_random\_game\_by\_jeremy\_labossiere},
\textsc{custom\_expulsion\_by\_chinbag},
\textsc{swizzeroony\_by\_increpare},
\textsc{lumberjack\_run\_by\_nickromancer},
\textsc{build\_a\_snowman\_v1.0\_by\_kimchi2012},
\textsc{impossible\_pushing\_-\_level\_2\_by\_stingby12},
\textsc{push\_pull\_undo\_redo\_by\_11pepi},
\textsc{trample\_by\_jonathan\_whiting},
\textsc{simple\_block\_pushing\_game\_by\_mirage-xel},
\textsc{marathon\_by\_2sman}, \textsc{simple\_puzzle\_by\_paul},
\textsc{maze\_by\_251119s},
\textsc{build\_a\_snowman\_v1.0\_by\_minjikim2013},
\textsc{skyroad\_by\_sorendipitous},
\textsc{interchange\_by\_pickten},
\textsc{combo\_by\_nicholas\_udell},
\textsc{path\_to\_the\_bee\_by\_jonbro},
\textsc{Game\_One}, \textsc{One\_Way\_Street},
\textsc{lasers\_by\_franklin\_p.\_dyer},
\textsc{on\_thin\_ice\_by\_gamez7\_with\_music\_by\_roccow},
\textsc{robot\_soccer\_by\_weiliuu}, \textsc{Bridge-toggle\_Maze},
\textsc{flipping\_by\_mlipmanh},
\textsc{bit\_match!\_by\_unknown\_author},
\textsc{Lime\_Richard},
\textsc{naomis\_challenge\_4\_game\_winter\_by\_techythanh},
\textsc{abocalipc\_zumbi\_br\_by\_robertjesus},
\textsc{puzz-not\_by\_stew\_hogarth},
\textsc{simple\_block\_pushing\_game\_by\_zacharybarbanell},
\textsc{go\_hunt\_$\psi$\texttrademark{}![beta]\_by\_puzzle00},
\textsc{Festive\_Lights}, \textsc{Rolling-block\_colour-zone\_mazes},
\textsc{build\_a\_snowball\_v2\_by\_coconana9010},
\textsc{remote\_sokoban\_by\_poorlydrawncactus},
\textsc{mr.hooper\_by\_yadel23},
\textsc{blind\_ninja\_by\_connorses},
\textsc{sticky\_v0.7\_by\_connorses},
\textsc{green\_by\_laindey\_denaige},
\textsc{cathartic\_colours\_by\_ronan\_tumelty},
\textsc{cow\_rancher\_by\_bagenzo},
\textsc{buny\_hoping\_by\_pickten}, \textsc{Solitaire},
\textsc{mini\_grapplenauts\_by\_jesse\_purvis},
\textsc{telekin\_by\_diego\_cathalifaud},
\textsc{Carnival\_Shooter!},
\textsc{minecraft\_v1.0.1\_by\_coconana9010},
\textsc{my\_game\_by\_dio7225},
\textsc{feed\_the\_cat\_by\_kim\_cerna},
\textsc{test\_golfer\_by\_simonskvara},
\textsc{maze\_by\_meena0303},
\textsc{move\_3\_crates\_next\_to\_eachother\_by\_alex\_giger},
\textsc{herding\_cats!\_by\_albert24082408},
\textsc{j\_by\_jack\_gilbert},
\textsc{schatkamer\_by\_jan-niestadt},
\textsc{arregla\_la\_vereda\_by\_marcos-do},
\textsc{test\_by\_finn\_coffey},
\textsc{simple\_block\_pushing\_game\_by\_meddlingcactus},
\textsc{cat\_making\_friends\_by\_wfykitty},
\textsc{tall\_player\_by\_henry-friedman},
\textsc{my\_game\_is\_better\_than\_your\_game\_by\_nahcirn},
\textsc{a\_very\_good\_puzzle\_by\_nahcirn},
\textsc{double\_message\_by\_sevansevan},
\textsc{weeeee\_by\_phuongstillhatework},
\textsc{testi\_by\_janisamuli},
\textsc{amor\_en\_tiempos\_de\_pandemia\_by\_i.enba},
\textsc{space\_invaders\_by\_e\_is\_cool\_\_\_edited\_by\_fz0718},
\textsc{two\_coats\_and\_crates\_by\_henry-friedman},
\textsc{ea\_sports\_fifa\_2018\_qatar\_world\_cup\_edition!\_by\_john\_q.\_futbol\_and\_ea\_sports\_of\_course},
\textsc{space\_invaders\_by\_solafson},
\textsc{platformer\_thing\_by\_thesalamaderboy},
\textsc{underpuzzle\_by\_pac2005},
\textsc{eventually\_this\_is\_gonna\_be\_zelda\_by\_soolseem},
\textsc{endless\_practice\_by\_plurmorant},
\textsc{player\_move,\_pause,\_badfish\_move,\_pause\_by\_jon-jon},
\textsc{title\_by\_author},
\textsc{rgb\_by\_michael\_straeubig},
\textsc{prototype\_by\_tebrown1011},
\textsc{full\_bloom\_by\_qwaffles},
\textsc{beat\_game\_test\_by\_connorses\_[loneship\_games]},
\textsc{Cell\_Division}, \textsc{test\_by\_katelyn\_delta},
\textsc{\_\_by\_jennybuckley},
\textsc{drunkard\_walk\_by\_unknown\_author},
\textsc{crossing\_the\_gap\_by\_barefootengineer1},
\textsc{marbles\_by\_jcmiller11},
\textsc{annoying\_skull\_by\_qwaffles}, \textsc{Colors},
\textsc{puzzletracker\_by\_puzzlescriptfan34},
\textsc{card\_by\_lintsungyueh},
\textsc{ice\_wolrd\_by\_scott\_hughes},
\textsc{test\_teleportation\_by\_eoniz},
\textsc{unlink\_by\_zacharybarbanell},
\textsc{pathfinding\_by\_madball}.
}

\paragraph{\textsc{Heldout-30} (30 games).}
The held-out evaluation set is disjoint from every training preset by
filename and by token-sequence hash. Drawn by stratified sampling
from the dedup pool to span low/mid/high rule counts.
{\small
\textsc{in\_the\_way\_by\_giovanni\_mota},
\textsc{cyberpunk\_2020\_by\_gzhao-jpg},
\textsc{my\_nana\_ate\_my\_kid\_brother\_by\_ruth\_williams},
\textsc{ice\_sliding\_by\_by\_ian\_cox},
\textsc{block\_game\_by\_emily\_dresden},
\textsc{kitten\_rescue\_by\_unknownprodigy-beep},
\textsc{kyles\_chesse\_cuisine\_by\_techythanh},
\textsc{the\_best\_2d\_game\_ever\_created\_by\_cwm\_gaming},
\textsc{monophobic\_multiban\_by\_increpare},
\textsc{charge\_the\_bot\_by\_tiddly5},
\textsc{build\_a\_snowman\_challenges\_by\_flyingfroakie},
\textsc{the\_single\_flame\_by\_hassan\_masood},
\textsc{pacman\_\_by\_cora}, \textsc{escape\_by\_shawn\_martin},
\textsc{megabot\_invades!\_by\_louis\_g},
\textsc{link\_by\_jeffjeff123456},
\textsc{\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_by\_ncrecc},
\textsc{watchers\_ritual\_by\_purpledragon17},
\textsc{gdd301\_demo\_game\_by\_emma\_hubbell},
\textsc{Surround\_Yourself\_With\_Dogs},
\textsc{haberdashery\_by\_lee2sman},
\textsc{ghosts\_by\_henry-friedman},
\textsc{a\_snowballs\_chance\_in\_hell\_by\_iznaut},
\textsc{hedgehogger\_by\_increpare},
\textsc{cat\_adventure\_2\_by\_whenyoucando}, \textsc{Hamiltwo},
\textsc{headless\_people\_problems\_by\_monakrom},
\textsc{break\_out\_of\_the\_mine\_by\_jja\_i.e.\_juan,\_jose\_\&\_andre},
\textsc{Heroes\_of\_Sokoban\_-\_Ancient\_Japan},
\textsc{angize\_by\_ali\_nikkhah}.
}

\subsection{Per-game \textsc{Train-14} breakdown}
\label{app:per-game}

Figure~\ref{fig:per-game-heatmap} shows the per-game BFS final
cell-error on \textsc{Train-14}, averaged over each game's authored
levels. Three observations beyond the aggregate numbers in
Table~\ref{tab:baselines-scaling14}:

\begin{itemize}
  \item \textsc{Travelling\_salesman} is hardest for every architecture
    (NCA-perstep best at 38\%); the rule set encodes an NP-hard
    combinatorial problem that no single-step world model is
    expected to solve perfectly.
  \item Several games (\textsc{nekopuzzle}, \textsc{notsnake},
    \textsc{sokoban\_basic}, \textsc{blocks}) are fit to zero error
    by the NCA family but fail badly for U-Net (10--25\%) and ViT
    (1--33\%). The architecture gap is consistent across diverse
    rule sets, not an averaged-out artifact.
  \item \textsc{scriptcross} is a clean ViT-failure case (71\% error
    vs.\ 11--15\% for the rest), illustrating the structural
    mismatch we discussed in \S\ref{sec:results-scaling}.
\end{itemize}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../figures/baseline_comparison/scaling_14_per_game_heatmap.pdf}
  \caption{\textbf{Per-game BFS final cell-error on
    \textsc{Train-14}.} Rows are games (sorted by NCA-shared error,
    ascending); columns are architectures. Single seed at 20k updates.
    Both NCA variants beat the three non-NCA baselines on the vast
    majority of games; ViT is the only architecture with a
    catastrophic failure on a small game (\textsc{scriptcross}).}
  \label{fig:per-game-heatmap}
\end{figure}

\subsection{Reproducibility}
\label{app:reproducibility}

The codebase, dataset caches, training launchers, and result
aggregators are released alongside the paper. Each run is reproduced
end-to-end by re-running its launcher on the same hardware
(single 24~GB consumer GPU). The full experimental log accompanies
the released code.
% Internal notes (deanonymised paths kept as comments for the
% camera-ready / reproducibility supplement):
%   - Launchers: nca_wm/scripts/run_*.sh
%   - Per-game/per-arch synth grid: nca_wm/scripts/run_per_game_arch_synth_grid.sh
%     summarised via summarize_per_game_arch_grid.py --all_authored_as_heldout
%     and figures via nca_wm/scripts/make_within_game_synth_artifacts.py
%     (logs land in nca_wm/logs_per_game_arch_synth/)
%   - Running-state docs: nca_wm/SCALING_RESULTS.md, nca_wm/ARCHITECTURE_REPORT.md,
%     nca_wm/RUNNING_REPORT.md, project_synth_recipe.md

\section{Per-game $\times$ per-architecture grid}
\label{app:per-game-arch}

To check how architecture choice interacts with game type, we train
one model per (game, architecture-bucket) cell on five games covering
distinct mechanic classes---%
\textsc{Microban} (single-step Sokoban-style box pushing: PuzzleScript's
basic push rule applies force to the cell directly in front of the
player, so a row of two boxes is \emph{not} chained-pushed by a single
action),
\textsc{Heroes\_of\_Sokoban} (multi-bracket character swap and
projectile travel),
\textsc{Bouncers} (non-axis-aligned looping projectile),
\textsc{nekopuzzle} (restricted-orientation movement), and
\textsc{Travelling\_salesman} (multi-bracket all-pairs)---against
four architecture buckets at $T{=}8$:
(A) pool ON, shared ($n_r{=}T$);
(B) pool ON, per-step ($n_r{=}1$);
(C) no-pool $+$ input-skip, shared;
(D) no-pool $+$ input-skip, per-step.
Each cell is trained on the game's authored level 0 only with the
canonical recipe (\S\ref{sec:experiments}); we evaluate BFS rollout
cell-error on the training level (L0) and on held-out levels
(L1$+$, mean) separately. Training budget is 15k updates per cell,
extended to 50k for \textsc{Travelling\_salesman} after observing
visible loss descent at 15k. Single seed. We also ran the same grid
on \textsc{sokoban\_basic} (a smaller variant of the same Sokoban
dynamics as \textsc{Microban}); the numbers are consistent with the
\textsc{Microban} row and we omit them from the figure to avoid
double-counting the same mechanic.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/heatmap_bfs.pdf}
  \caption{\textbf{Per-game $\times$ per-architecture BFS cell-error.}
    Rows: games. Columns: architecture buckets at $T{=}8$. Left panel:
    training-level (L0) cell-error. Right panel: held-out-levels (L1$+$
    mean) cell-error. Cells annotate the percentage. Bucket~D
    (no-pool $+$ input-skip $+$ per-step) wins held-out on five of six
    games. Bucket~A loses dramatically on \textsc{Travelling\_salesman}
    L0 (45.1\%) despite identical train loss to bucket~D (which fits
    L0 to 0\%); see ``F3 sharpened'' below.}
  \label{fig:per-game-arch-heatmap}
\end{figure}

\paragraph{Train-loss-vs-rollout-error decoupling, sharpened.}
\label{app:F3-sharpened}
A model with the lowest per-step train loss is not the model with the
lowest autoregressive rollout error. Concretely, in the over-capacity
regime we observe per-step models with $5\times$ lower train loss but
$3$--$4\times$ higher 50-step rollout error: small but systematic
per-step errors compound destructively under autoregression. The
per-game grid pushes the gap to its loudest form yet.
On \textsc{Travelling\_salesman} at 50k updates all four architecture
buckets converge to within $1\%$ of the same train loss
($1.70\times 10^{-3}$ to $1.72\times 10^{-3}$), yet L0 BFS cell-error
splits $0\% / 0\% / 26.9\% / 45.1\%$ across buckets D / C / B / A.
Bucket~A's L0 rollout is in fact \emph{worse} at 50k than at 15k
($45.1\%$ vs.\ $24.0\%$) despite a $68\times$ lower train loss---%
additional optimisation on a per-step minimum that compounds
destructively under autoregressive feedback. Buckets~C and D, which
replace global pool features with input-skip re-injection, converge to
the same train loss with a stable rollout. The pool-ON cells thus
appear to overfit per-step prediction in a way that does not survive
auto-regressive feedback.

\paragraph{Held-out gap and the synth-data prediction.}
Held-out cell-error (right panel of Fig.~\ref{fig:per-game-arch-heatmap})
remains substantial across every cell ($7.3\%$--$56.8\%$). This is the
\emph{within-game} OOD-levels axis---a complementary, lesser form of
generalization than the OOD-rules / latent-sampling / inverse-fit
results in Appendix~\ref{app:ood}. Prior single-game work strongly
predicts that synthetic levels close most of this gap: training
synth-only \textsc{sokoban\_basic} ($w{=}7$) reached $0\%$ cell-error
on the 155 held-out \textsc{Microban} / \textsc{Microban\_I} authored
levels, and multi-grid synth on \textsc{nekopuzzle} closed a
$3$--$8\%$ gap to $0.3\%$ once the synth widths included the authored
aspect ratio. Both signals suggest the right-panel gap here is data
scarcity, not an architectural ceiling.
% Prior-work pointers (deanonymised; kept as comments):
%   - nca_wm/RUNNING_REPORT.md (single-game synth-only Sokoban run)
%   - nca_wm/SCALING_RESULTS.md (multi-grid nekopuzzle synth run)

\paragraph{Synth-only $\to$ all-authored generalization (running).}
\label{app:synth-only}
The within-game-generalization probe replaces each cell's authored
training set with $K{=}128$ synthetic levels generated at the
\emph{smallest-by-area} authored size of the target game and evaluates
on \emph{every} authored level (no L0 / heldout split: all authored
levels are out of distribution to the training set). We anchor on the
smallest authored size because (i) size-up generalization carries
synthetic small-grid training to bigger authored levels (Sokoban
$w{=}7 \to 0\%$ on 155 \textsc{Microban}/\textsc{Microban\_I}
authored levels in prior work) while size-down does not; (ii) using
a single size keeps the per-size data density at $K{=}128$, which
empirically matters: an early multi-grid variant
($K / n_{\text{sizes}}$ per size, $\approx 16$ levels per size for
Microban) plateaued at $\approx 8\%$ BFS heldout while the same recipe
at fixed $K{=}128$ on a single small size reached $0.60\%$ BFS,
$0.02\%$ 1-step. We also tried an auto-bounding-box variant that
picks $\max W \times \max H$ for each game; it failed at $\approx 9\%$
BFS because many authored levels are much smaller than the bounding
box, and the synth distribution missed them. Smallest-by-area (rather
than per-axis $\min W \times \min H$) is used so the chosen size is
an actually-existing authored shape rather than a per-axis composite
(\textsc{Microban}'s per-axis minimum is $6{\times}6$, but no authored
level is $6{\times}6$; smallest-by-area picks $6{\times}7$, which
appears in three authored levels).

The recipe trains on $K{=}128$ synthetic levels at the chosen size for
$30{,}000$ updates with the same four architecture buckets at $T{=}8$,
with synthetic-level generation in \emph{evolve} mode and a small
auxiliary token-decoder loss. To force the GA to spread the $K$-level
budget across the game's rule set rather than collapsing onto a single
easy mechanic, we enable rule-coverage-driven evolution: per-step
rule-firing telemetry biases the fitness to
$\text{BFS-iterations} + 100 \cdot |\text{unique rules fired}|$, and
at the end of evolution we greedily pick the $K$ levels that maximize
the \emph{union} of rules covered (rather than top-$K$ by individual
fitness). For small-rule-set games (e.g.\ Sokoban-class) this is a
no-op; for multi-bracket games (Heroes\_of\_Sokoban,
Travelling\_salesman) it materially changes which $K$ levels enter
training. We also report the iterations-only baseline (no coverage
weighting, no union-greedy selection) as a side-by-side ablation
column in Table~\ref{tab:within-game-synth}. The pre-registered
prediction follows in \S\ref{app:within-game-prediction}. If pure
synth fails on a game we fall back to seeding from one or a handful
of authored levels alongside synth---a lesser form of within-game
generalization that still tests few-shot extrapolation.
% Internal pointers (paths kept as comments for the reproducibility
% supplement; they reference deanonymised tooling, so they should not
% appear in the submission body):
%   - Launcher: nca_wm/scripts/run_per_game_arch_synth_grid.sh
%   - Summariser: nca_wm/scripts/summarize_per_game_arch_grid.py
%       --all_authored_as_heldout
%   - Logs land in nca_wm/logs_per_game_arch_synth/
%   - Validated post-1fa557d on Microban, bucket D, 30k updates;
%     see project_synth_recipe.md for full validation history.
%   - Recipe flags:
%     --synthetic_levels 128 --synthetic_w <min_W> --synthetic_h <min_H>
%     --synthetic_fallback_dynamics --synthetic_no_a_count_max 5
%     --synthetic_mode evolve --token_decoder_loss_weight 0.1
%     --synthetic_track_rules_fired --synthetic_rule_coverage_weight 100
%     --synthetic_coverage_select_topk

\papertodo{Fill the synth-only column once the sweep completes:
regenerate the per-game/per-architecture heatmap as a 4-panel figure
(L0 authored vs.\ heldout authored vs.\ synth-only all-authored, with
synth-train-loss as the bonus panel) and update
Table~\ref{tab:within-game-synth}.}

\papertodo{If synth-only beats authored-L0-only on \textsc{Microban}
held-out (Sokoban prior says yes), Heroes\_of\_Sokoban (multi-bracket
char-swap; less certain), and Bouncers (looping projectile; uncertain),
that closes the within-game OOD-levels claim across three distinct
mechanic classes. \textsc{Travelling\_salesman} is the canonical hard
case: if the synth-only run reproduces the F3 sharpening (same train
loss, $40{+}$pp rollout split between buckets) it is the cleanest
demonstration of the per-step-vs-AR decoupling we report, since no
authored memorization remains to confound the comparison.}

\papertodo{\textbf{Extensions.} (i) Add a depth axis $T \in \{4, 16, 32\}$
to test whether bucket A's pool-driven AR-rollout failure shrinks at
larger $T$; (ii) add games covering gravity-chain (\textsc{MazezaM}),
counter / state aggregation (\textsc{Modality}), and length-state
(\textsc{Lime\_Rick}) mechanics; (iii) re-run the existing five cells
with three seeds, since prior work (F8, \S\ref{app:ceiling})
showed per-seed variance can dwarf recipe deltas on similar
single-game multi-grid tasks.}

\section{Within-game generalization from synthetic levels}
\label{app:within-game}

The fidelity numbers in Table~\ref{tab:fidelity} measure cell-error
on the same authored levels the model trained on. To probe whether
the rule-conditioned NCA learns rule structure rather than per-state
outcomes within a single game, we strip every authored level out of
the training set and substitute $K{=}64$ purely-synthetic levels per
game generated by an evolutionary search at the game's authored
max-dim. Evaluation is unchanged: BFS / A* / random-action rollouts
on \emph{every} authored level. Five games span distinct mechanic
classes (\textsc{Microban}, \textsc{Heroes\_of\_Sokoban},
\textsc{Bouncers}, \textsc{nekopuzzle},
\textsc{Travelling\_salesman}); each game is trained four times to
cover the architecture buckets in Appendix~\ref{app:per-game-arch}
(pool $\times$ weight-sharing at $T{=}8$). The recipe and launcher
are documented in \S\ref{app:synth-only}.

Pure-synthetic supervision is the strongest available within-game
OOD-levels probe: every authored level is genuinely held-out from
training; the only signal the model has about the rule set is the
tokenized rule text plus rollouts on synthesized grids. A model that
has memorized authored states cannot succeed here.

\begin{table}[t]
  \centering
  \caption{\textbf{Synth-only $\to$ all-authored generalization (running).}
    Each cell is the mean BFS rollout cell-error across every authored
    level of the game, after training on $K{=}128$ purely-synthetic
    levels at the smallest-by-area authored size of the game.
    No authored level enters training. Best architecture bucket per
    game is bolded. Cells marked ``—'' are pending the full sweep;
    the displayed Microban-D number is the anchor result described
    below.}
  % Auto-regenerated from nca_wm/logs_per_game_arch_synth/ by
  % nca_wm/scripts/make_within_game_synth_artifacts.py
  \label{tab:within-game-synth}
  \small
  \input{figures/per_game_arch_synth/within_game_synth_table.tex}
\end{table}

\paragraph{Anchor result.}
A pilot run of the validated recipe (\S\ref{app:synth-only}) on
\textsc{Microban}, bucket D ($T{=}8$, no-pool $+$ input-skip,
per-step), at the smallest-by-area authored size, $K{=}128$,
$30{,}000$ updates produces the rich-metric numbers in
Table~\ref{tab:within-game-synth-anchor}. Five of the ten authored
levels land at $0\%$ BFS exactly. The $0.02\%$ teacher-forced figure
is in fact $5\times$ \emph{lower} than the authored-only baseline
of $0.11\%$ at $10{,}000$ updates
(Table~\ref{tab:baselines-sokoban}), demonstrating that synth-only
training with a coverage-driven GA produces a \emph{more accurate}
per-step rule predictor than authored-only training, with the BFS
rollout still close to authored (within $5\times$). This is the
within-game OOD-levels evidence the section advertises: the
rule-conditioned NCA does learn rule structure rather than
per-authored-state outcomes within a single game.

\begin{table}[t]
  \centering
  \caption{\textbf{Anchor run for the synth-only recipe.} Mean per-cell
    error across all 10 authored \textsc{Microban} levels (no level
    enters training).}
  % Auto-regenerated from
  %   nca_wm/logs_per_game_arch_synth/Microban__D_d8_w7_k128_30k
  % by nca_wm/scripts/make_within_game_synth_artifacts.py
  \label{tab:within-game-synth-anchor}
  \small
  \input{figures/per_game_arch_synth/anchor_microban_d.tex}
\end{table}

\paragraph{Pre-registered prediction for the remaining cells.}
\label{app:within-game-prediction}
Prior single-game evidence is sharp on at least the Sokoban-class
subset (cf.\ the Sokoban $w{=}7 \to 0\%$ on 155
\textsc{Microban}/\textsc{Microban\_I} levels prior;
\textsc{nekopuzzle} multi-grid closing a $3$--$8\%$ authored gap to
$0.3\%$). We expect (i) Sokoban-class games (\textsc{Microban}) to
land at $\leq 1\%$ BFS across all four buckets, replicating the
\textsc{Microban}-D anchor; (ii) \textsc{Bouncers} and
\textsc{Heroes\_of\_Sokoban} to show non-trivial transfer with the
architecture gap (D vs.\ A) at least as large as in the authored
case; (iii) \textsc{Travelling\_salesman} to fail under pure synth
supervision at $5{\times}5$ and require the authored-seeded fallback,
since the all-pairs combinatorial structure of larger TSP grids is
not present in the small-grid synth distribution.

\paragraph{Decoupling reprise without memorization.}
On \textsc{Travelling\_salesman}, the per-game grid in
Appendix~\ref{app:per-game-arch} already showed all four
architecture buckets converging to within $1\%$ of the same
synthetic-style train loss while their L0 BFS cell-error split
$0\% / 0\% / 26.9\% / 45.1\%$. The synth-only setup pushes that
test further: with no authored memorization available, any
train-loss parity across buckets is forced to come from learning
rule structure, so an even larger AR-rollout split would be the
cleanest available signal that pool-driven over-fitting on per-step
prediction destabilizes auto-regressive feedback.

\section{Architecture findings}
\label{app:arch-findings}

The architecture comparison in \S\ref{sec:results-baselines} reports a
single number per architecture; this appendix expands the underlying
architectural findings. We label the findings F1--F3 to align with
the running architecture report released alongside the code.

\paragraph{(F1) Global pool is load-bearing; axis-cummax targets a
narrower rule class.}
A bare $3{\times}3$ convolution can only propagate one cell of context
per NCA step. Several PuzzleScript rule patterns reference cells
arbitrarily far apart along an axis or anywhere on the level
(\texttt{[X | ... | Y]}, \texttt{[X][Y]}). Without pool features the
NCA needs $\Omega(\max(H,W))$ steps just to surface non-local
information. The per-feature ablation in
Table~\ref{tab:ablation-pool} isolates which pool flavour does the
work: removing global-pool alone is as costly as removing all three
($+145\%$ relative BFS error), while removing axis-cummax has no
measurable effect on \textsc{sokoban\_basic} (its target patterns---%
axis-aligned \texttt{[X | ... | Y]} matches---do not appear in
that game's rule set). On a 19-game gallery preset and a controlled
\textsc{collapse} no-pool ablation, turning pool off costs both
convergence speed and final loss, with the no-pool model collapsing
into the identity-prediction minimum even at $4{\times}$ the depth.

\paragraph{(F2) Shared NCA-body weights are the right inductive bias.}
The PuzzleScript engine has two natural levels of iteration: an
inner ordered list of rules applied once each per tick, and an outer
\texttt{again} loop that re-runs the rule list until the state stops
changing. We factor $T = n_\ell \cdot n_r$ accordingly. On a
single-game depth sweep ($T \in \{2, 4, 8, 16\}$), shared $T{=}8$
reaches $5.5\times$ lower train loss with $7\times$ fewer parameters
than the per-step body at the same depth; shared $T{=}2$ at
$14\times$ fewer parameters than the deepest per-step variant
matches it on oracle-action rollouts. Hierarchical $n_r > 1$ sits
between these extremes (\S\ref{app:hierarchy}).

\paragraph{(F3) Train loss decouples from autoregressive rollout error.}
\label{app:F3}
A model with the lowest train (per-step) loss is not the model with
the lowest autoregressive rollout error. Concretely, in the
over-capacity regime we observe per-step models with $5\times$ lower
train loss but $3$--$4\times$ higher 50-step rollout error. The
mechanism is small but systematic per-step errors compounding
destructively under autoregression. Reporting only best-loss hides
the rollout-drift cost of going deeper or wider.
Appendix~\ref{app:per-game-arch} reports a sharpened version of this
finding on a per-game $\times$ per-architecture grid: on
\textsc{Travelling\_salesman} all four buckets converge to within
$1\%$ of the same train loss but split
$0\% / 0\% / 26.9\% / 45.1\%$ on L0 BFS cell-error.

\section{Hierarchical weight sharing}
\label{app:hierarchy}

\papertodo{Add hierarchy figure/table: $T{=}16$ varying
$n_r \in \{1, 2, 4, 8, 16\}$ on \textsc{microban} multi-grid synth
and on the multi-game \textsc{Train-14} preset, reporting
change-error and parameter count.}

\section{Additional related work}
\label{app:related}

\paragraph{Adaptive computation.}
PonderNet~\cite{banino2021pondernet} learns a per-input halting
distribution over a recurrent module, with a KL prior over the number
of iterations. Adaptive Computation Time~\cite{graves2016act} and
Universal Transformers~\cite{dehghani2018universal} are predecessors.
Our adaptive-halt experiments
(Appendix~\ref{app:halting}) adapt PonderNet to NCA world models
with a shared body, so that ``halt at step $k$'' coherently asks the
same rule set to converge in $k$ iterations rather than selecting
between $k$ different models.

\section{Adaptive halting}
\label{app:halting}

We compare four halting modes and a fixed-depth baseline on a custom
$1$-rule \textsc{varislide} game in which a single \texttt{again}-driven
slide rule pushes the player up to $d \in \{1, 2, 3, 4, 6, 8, 12, 16\}$
cells per action across grid widths $\{6, 8, 10, 12, 16\}$. Modes:
\emph{ponder} (PonderNet ponder loss with learned halt head + KL
prior), \emph{uniform} (per-step loss equally weighted across all
$T$ steps), \emph{argmax-ST} (straight-through estimator on
$\arg\max_k p_k$), and \emph{convergence-ST} (halt at inference when
consecutive predictions stop changing, no learned head).

\papertodo{Add halting figure: per-step error trajectory under each
mode plus per-instance halt distribution as a function of slide
distance.}

Across these modes we observe two phenomena. First, on tasks where
the dynamics fit cleanly in $\approx 1$--$2$ NCA steps (small-grid
varislide, \textsc{collapse}), all halting modes converge near the
dynamics-floor within their effective depth and drift past it;
convergence-ST recovers near-oracle performance via inference-time
stopping. Second, on the multi-grid synthetic regime where
memorization is unavailable, \emph{none} of the halting modes
reliably learn the underlying rule (\S\ref{app:ceiling}); the halt
head at best degenerates to halt-at-$k{=}1$ + identity prediction.

\section{Multi-grid synthetic data ceiling}
\label{app:ceiling}

When we move from a single authored level to the multi-grid synthetic
data regime ($\{6,8,10,12,16\}$ widths $\times$ 64 levels each),
train change-error climbs by $1000\times$
($5 \times 10^{-5} \to 0.05$--$0.10$), confirming that the model can
no longer 1-shot-memorize per-state outcomes. The model attempts to
learn the rule but plateaus: 50k updates at $d_h{=}256$ ($5\times$
more updates, $2\times$ wider) does not move the needle. Multi-seed
verification at $T \in \{8, 16, 32, 64\}$ shows per-seed argmax
variance dwarfs the recipe signal and best-loss is essentially
identical across all configurations. We discuss candidate fixes in
\S\ref{sec:discussion}.

\section{Generalization to unseen games}
\label{app:ood}

The 30-game stratified held-out set is disjoint from every training
preset by name and by token-sequence hash. We probe generalization
along three axes: held-out latent geometry, symbolic decoder
reconstruction and sampling, and frozen-NCA inverse fitting from
observed transitions.

\subsection{Held-out latent geometry as we scale games}
\label{app:scaling-curve}

For each held-out game we encode its tokens through the trained
encoder, compute cosine distance to every training game's encoded
slot, and report the smallest of those distances---the distance
to the held-out game's \emph{nearest training-game neighbour} in
slot space. We use the flattened
$K \cdot d_{\text{slot}} = 1024$-dim slot vector, and the median
across the 21 of 30 held-outs whose tokens fit within the smallest
training preset's maximum sequence length.

We treat this as a \emph{diagnostic}, not a quality metric. A
distance of zero is collapse, not perfect transfer (the held-out
game has been mapped onto an existing training point). A large
distance is off-manifold, also bad. The number is also naturally
expected to shrink as the training preset grows---with more
games the chance that some training game lies near any given
held-out target rises mechanically, independent of representation
quality. Predictive OoD performance---per-cell error of the dynamics model
on the held-out set's authored-level transitions, against an
identity baseline---is the load-bearing claim for unseen-game
generalization; the geometry numbers below are reported only for
context.

\begin{table}[t]
  \centering
  \caption{\textbf{Held-out latent geometry as we scale training
    games.} Cosine distance, in flattened-slot space, from each
    held-out game's encoded slot to its nearest training-game
    neighbour. All runs use the rule-attention NCA body, $K{=}16$
    slots, $d_{\text{slot}}{=}64$, $T{=}8$ NCA steps, batch 32, 150k
    Adam updates. The held-out set is the same 30-game stratified
    sample for every row.}
  \label{tab:scaling-curve}
  \small
  \begin{tabular}{l r l r}
    \toprule
    preset & $n_{\text{games}}$ & decoder & median distance to NN train game \\
    \midrule
    \textsc{Train-14}             & 14 & off & $0.077$ \\
    \textsc{Train-94}             & 94 & off & $0.215$ \\
    \textsc{Train-94+dec}         & 94 & on  & $\mathbf{0.153}$ \\
    \bottomrule
  \end{tabular}
\end{table}

At 94 training games the encoder-only median is larger than at
$14$ ($0.215$ vs.\ $0.077$); the 14-game number is partly a
consequence of the held-out games being homogeneous push-puzzles
that sit close to any of several push-puzzle training games at
that scale. The 94-game decoder-on row drops to $0.153$, but
because of the diagnostic caveats above we do not read this as
evidence the decoder objective improves transfer; it could equally
indicate decoder-induced collapse onto training neighbours.
Whether those neighbours are \emph{mechanically} related to the
held-out target is a separate question---two example matches in
\S\ref{app:inverse-fit} suggest the geometry is not pure
collapse, but we have not enumerated the relation across the full
held-out set.

\subsection{Symbolic decoder: reconstruction and sampling}
\label{app:autoencoder}

The joint encoder + NCA + slot-token decoder is trained on
\textsc{Train-94} for 150k updates with $\beta = 1.0$ on the decoder
loss. The decoder converges to per-token cross-entropy
$\approx 2 \times 10^{-9}$ within the first 5k updates and holds
there throughout, adding 1.18M parameters on top of the 9.89M
world-model parameters and roughly 20\% to per-step wall-clock.

\paragraph{Token reconstruction accuracy} (per-position match rate
between the decoder's argmax prediction and the target token under
teacher forcing on training games): perfect reconstruction on
$76/94$ training games and a median per-position match of $1.000$
(mean $0.906$, min $0.276$). The slot bottleneck retains the
information needed to recover the full symbolic specification on the
majority of the corpus; the imperfect mean is dominated by a small
tail of long, sprite-rich games that exhaust the decoder's
positional budget.

\paragraph{Engine-load success rate} (fraction of decoded token
sequences that, after detokenization to PuzzleScript source, parse
and compile via the JS engine): $15/25$ engine-loadable PuzzleScript
programs ($60\%$) from greedy autoregressive decoding from random
Gaussian-fit slots; $9/9$ engine-loadable programs along a 9-point
linear interpolation between two endpoint training games
(\textsc{Ebony\_\&\_Ivory} $\rightarrow$ \textsc{rigidfail1};
$100\%$). The generations contain plausible push rules of the form
\verb"[ > Obj2 | Obj4 ] -> [ > Obj2 | > Obj4 ]",
\texttt{late}-prefixed rules, and per-object $5{\times}5$ pixel
sprites with palettes consistent across the legend, the collision
layers, and the rule body.

\subsection{Inverse-fitting unobserved games from transitions}
\label{app:inverse-fit}

Because the dynamics model is conditioned on a continuous game
representation, we can freeze the NCA parameters and infer an
unobserved game embedding from transition data. Concretely, we
freeze the world model and decoder $(\theta, \psi)$ and treat
$z \in \mathbb{R}^{K \times d_{\text{slot}}}$ as the only free
variable: the encoder $\mathrm{Enc}_\phi$ is bypassed entirely and
the optimised $z$ enters the world model through a slot-override
path. We initialise $z$ from a small Gaussian around the empirical
training-slot mean and optimise the per-cell next-state BCE used
during training, summed over the held-out target's observed
transitions, for 2{,}000 Adam steps at $\text{lr} = 5 \times 10^{-3}$
with batch size 32. Targets are drawn from the deduped pool and
verified disjoint from training by name and by token-hash. After
fitting, we report the cosine distance from the recovered $z$ to
its nearest training-game slot---``nearest train'' in
Table~\ref{tab:inverse-fit} means the closest training-game slot in
the flattened-slot space, not a comparison against a held-out
reference encoding of the target.

\begin{table}[t]
  \centering
  \caption{\textbf{Inverse-fit recovery from OOD transitions.}
    Final per-cell BCE on the observed transitions of three
    held-out games, and the cosine distance from the optimised slot
    to its nearest training-game neighbour (with that neighbour's
    name).}
  \label{tab:inverse-fit}
  \small
  \begin{tabular}{l c c l}
    \toprule
    held-out target & final BCE & dist.\ to NN train & nearest train game \\
    \midrule
    \texttt{monophobic\_multiban} & $\mathbf{8.4 \times 10^{-4}}$ & $0.389$ & \texttt{run\_em\_over} \\
    \texttt{cat\_adventure\_2}    & $\mathbf{8.4 \times 10^{-4}}$ & $0.394$ & \texttt{neko\_puzzle\_by\_increpare} \\
    \texttt{in\_the\_way}         & $7.2 \times 10^{-3}$         & $0.477$ & \texttt{run\_em\_over} \\
    \bottomrule
  \end{tabular}
\end{table}

The optimisation drives BCE down two-to-three orders of magnitude on
every target, so the frozen NCA can fit OoD dynamics through the
latent variable. The reported BCE is on the same transition tape
the slot was optimised against, however, so it measures the
expressive capacity of $(\theta, \psi)$ plus the optimisation
landscape, not held-out generalisation; an honest upgrade would
hold out a fraction of the tape, fit on the rest, and report
test-tape BCE.

On two of three targets the recovered slot's nearest training-game
neighbour matches a plausible mechanical sibling:
\texttt{cat\_adventure\_2} (a push-puzzle) lands closest to
\texttt{neko\_puzzle\_by\_increpare} (also a push-puzzle), and
\texttt{monophobic\_multiban} (a multi-character chase mechanic)
lands closest to \texttt{run\_em\_over} (also multi-character /
chase). We do not yet have a systematic enumeration of how often
the nearest neighbour is mechanically related across the full
held-out set; the two matches above are illustrative.
