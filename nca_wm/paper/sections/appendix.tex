\section{Implementation details}
\label{app:impl}

\subsection{Model construction}

All models are implemented in JAX/Flax with the Adam optimizer. The
slot encoder is identical across architectures (see
\S\ref{sec:method}). The hidden grid dimension $d_h$, the cell
update structure, and the readout heads are also shared; only the
spatial-update body (rule-attention NCA, CNN, U-Net, or ViT) varies.

\subsection{Tokenization}

Each game's tokenized representation is a sequence of integers from
a $\approx 140$-symbol vocabulary covering: object names, layer
references, rule operators, action types, the 5$\times$5 sprite RGBA
values when sprite tokens are enabled, and per-rule kernel
separators. A single token sequence per game serves as the encoder
input.

\subsection{Dataset provenance and training suites}
\label{app:data-provenance}

After filename and token-hash deduplication, all training and held-out
splits are defined by explicit game-name lists.

\paragraph{Single-game Sokoban level provenance.}
The 10 authored Sokoban levels used for the single-game evaluation
(Sokoban authored block, Table~\ref{tab:baselines-arch}) are drawn from the
\textit{Microban} level pack, a community-authored set of Sokoban
puzzles. \textit{Microban} shares its rule set 1-to-1 with
PuzzleScript's basic Sokoban game (the \textit{Simple Block Pushing
Game} demo by David Skinner, source filename \texttt{sokoban\_basic});
the only thing it adds is additional authored levels for the same
dynamics. Wherever this appendix references \textit{Microban} or the
basic-Sokoban variant explicitly (e.g.\ in the per-game $\times$
per-architecture grid in \S\ref{app:per-game-arch}), it refers to
this same Sokoban rule set.

Game titles below are pulled from each source \texttt{.txt}'s
\texttt{title} field (with \texttt{(Author)} or
\texttt{(Author; filename)} added wherever a title or
title$+$author pair is shared by multiple games in the corpus). They
are auto-generated from
\texttt{nca\_wm/paper/scripts/extract\_game\_titles.py}.

\paragraph{\textsc{Train-14} (14 games).}
\textit{neko puzzle (lexaloffle; nekopuzzle)}, \textit{Notsnake},
\textit{blocks.}, \textit{Simple Block Pushing Game (David Skinner;
sokoban\_basic)}, \textit{Match 3 Block Push}, \textit{Zen Puzzle
Garden}, \textit{Multi-word Dictionary Game}, \textit{Kettle},
\textit{Travelling salesman}, \textit{Collapsable Sokoban},
\textit{Love and Pieces}, \textit{Simple Action Example (increpare;
actiontest)}, \textit{ScriptCross}, \textit{Modality}.

\paragraph{\textsc{Train-59} (59 games).}
\textsc{Train-59} is the curated Gallery preset (filter: $\le 20$
rules, $\le 20$ objects, $\le 30$ max level area, $\le 30$ levels)
extending \textsc{Train-14} with 45 further Gallery games. One
preset entry (\texttt{blank}) is dropped at load time as
token-identical to the basic-Sokoban demo, leaving 59 games. The
deduplicated form (drop \texttt{blank} + two further token-collapsed
duplicates, \textit{Microban} and \texttt{twolittlecrates4}) is the
57-game gallery base referenced in \textsc{Train-94} and
\textsc{Train-199} below.

\paragraph{\textsc{Train-94} (94 games).}
The \textsc{Train-94} preset is the deduplicated 57-game gallery
base (above) plus 37 stratified additions drawn from the
3{,}474-game dedup pool (12 low, 15 mid, 8 high, 2 xhigh rule-count
buckets).

\textit{Deduplicated 57-game gallery base:}
{\small
\textit{neko puzzle (lexaloffle; nekopuzzle)}, \textit{Notsnake},
\textit{blocks.}, \textit{Simple Block Pushing Game (David Skinner;
sokoban\_basic)}, \textit{Match 3 Block Push}, \textit{Zen Puzzle
Garden}, \textit{Multi-word Dictionary Game}, \textit{Kettle},
\textit{Travelling salesman}, \textit{Multiple Patterns}, \textit{the
undertaking}, \textit{Wrapping Recipe}, \textit{Collapsable Sokoban},
\textit{Love and Pieces}, \textit{Simple Action Example (increpare;
actiontest)}, \textit{BROKEN Naive Extended Bodies},
\textit{ScriptCross}, \textit{Modality}, \textit{constellation z},
\textit{Random Robots}, \textit{Simple Action Example (increpare;
againexample)}, \textit{Spriteaporter}, \textit{Robot Spawner},
\textit{Simple Block Pushing Game (increpare; twolittlecrates1)},
\textit{Extended Body Example: 1 Player, 1 Extended Object},
\textit{Long Haul Space Flight}, \textit{Simple Block Pushing Game
(increpare; leftrightnpcs)}, \textit{Simple Block Pushing Game
(increpare; twolittlecrates2)}, \textit{Simple Block Pushing Game
(increpare; twolittlecrates3)}, \textit{Octat}, \textit{Lunar
Lockout}, \textit{Stairways}, \textit{the art of cloning},
\textit{Extended Body Example: 1 Player, Multiple Extended Objects},
\textit{One player, unlimited rigidbodies}, \textit{Some lines were
meant to be crossed}, \textit{Block Faker}, \textit{Pushing It},
\textit{2D Whale World}, \textit{MazezaM}, \textit{Ebony \& Ivory},
\textit{Singleton Traffic}, \textit{maze generator},
\textit{Slidings}, \textit{Lime Rick}, \textit{Cliched River Puzzle},
\textit{Midas}, \textit{Extended Bodies: Multiple Parallel Players,
Multiple Extended Object}, \textit{Take Heart Lass}, \textit{Broken
Rigid Body example}, \textit{Lightdown}, \textit{The observer's
paradox}, \textit{Many parallel players, unlimited rigidbodies},
\textit{MC Escher's Equestrian Armageddon}, \textit{Smother},
\textit{It Dies In The Light}, \textit{Pushcat Jr}.
}

\textit{Low-rule additions (12):}
{\small
\textit{Time-reversed Microban}, \textit{Clone Cloned Clones!},
\textit{Bell, Book, and Candle}, \textit{Zombie Bedtime},
\textit{Dot Puzzle 3.1}, \textit{Cat in a box}, \textit{No Name Yet},
\textit{Bit Treats}, \textit{Carnival Shooter!}, \textit{SokoBee
simples}, \textit{Shadow}, \textit{My Game (Calvin McNamara)}.
}

\textit{Mid-rule additions (15):}
{\small
\textit{neko puzzle (lexaloffle; neko\_puzzle\_by\_increpare)},
\textit{Division}, \textit{El laberinto dorado}, \textit{Simple
Block Pushing Game (David Skinner;
simple\_block\_pushing\_game\_by\_maximyzer)}, \textit{snowman game},
\textit{The Adventurer}, \textit{BoxoB},
\textit{``\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_'' (\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_by\_bagenzo)},
\textit{Pokemon GOOOOO}, \textit{Simple Foxes}, \textit{Run em' over},
\textit{Encuentra a tu amigo}, \textit{Build A Snowman v2.0},
\textit{Constellation}, \textit{Avaruus tunkeutujat}.
}

\textit{High-rule additions (8):}
{\small
\textit{Union Move}, \textit{Cyber Sokoban}, \textit{game},
\textit{nabokoS 3 - Tech Demo}, \textit{buny hoping},
\textit{Drunkard walk}, \textit{Directional Insanity}, \textit{tron}.
}

\textit{Xhigh-rule additions (2):}
{\small
\textit{Venganza I}, \textit{Spell}.
}

\paragraph{\textsc{Train-199} (199 games).}
\textsc{Train-199} reuses the 57-game gallery base and extends it
with 142 stratified additions drawn from the dedup pool under the
same filter. \textsc{Train-94} and \textsc{Train-199} are parallel
sweeps from the same base, not nested.

\textit{142 stratified additions:}
{\footnotesize
\textit{Bit Treat}, \textit{ERROR 1}, \textit{Threat Level Midnight},
\textit{the magic broom}, \textit{Davib}, \textit{Cat Adventure
Deluxe! Demo.}, \textit{Opposition}, \textit{MY EYES!!!},
\textit{a game}, \textit{Bombero en acción}, \textit{Displacement},
\textit{Bittle Bat}, \textit{Match Threekoban}, \textit{My Game
(Amanda Howell)}, \textit{Puzzle}, \textit{Super Mario Nazi Killer},
\textit{Ditto}, \textit{The Far Away Danish Pastry is always
Deliciouser.}, \textit{Prototype 1}, \textit{Herding Cats!!!1!},
\textit{Robot Run}, \textit{sokobad}, \textit{Block Pushing Game
Tutorial 2-4}, \textit{2to}, \textit{Blockpush}, \textit{Simple Block
Pushing Game (David Skinner;
simple\_block\_pushing\_game\_by\_zaydiscool777)},
\textit{neko puzzle (lexaloffle; neko\_puzzle\_by\_increpare)},
\textit{NO FORBIDDEN SYMBOLS}, \textit{All These Damn Crates},
\textit{Bahçıvanın Çiçekleri}, \textit{T-Rex Hot Chocolate},
\textit{Zombie Blocks}, \textit{The Poop Game}, \textit{Hanoi},
\textit{Feed the flames}, \textit{Match}, \textit{Monster block push
game}, \textit{Welcome to Flatland}, \textit{Switch switcher},
\textit{snow day}, \textit{ambiguity/generalization prototype},
\textit{Avain\_peli}, \textit{Midnight Snack}, \textit{de+a+ch},
\textit{variations on constellation z}, \textit{Rabbit meets Camel},
\textit{Golem Five}, \textit{neko puzzle (Manuel)}, \textit{Puzzle
Script Random Game}, \textit{Custom Expulsion}, \textit{SWIZZEROONY},
\textit{Lumberjack Run}, \textit{Build a Snowman v1.0 (Masataro
Kimchi)}, \textit{Impossible pushing - Level 2}, \textit{push pull
undo redo}, \textit{Trample}, \textit{Simple Block Pushing Game
(Stephen Lavelle; simple\_block\_pushing\_game\_by\_mirage-xel)},
\textit{Marathon}, \textit{Simple Puzzle}, \textit{Maze (Sophie Wang)},
\textit{Build a Snowman v1.0 (Minji kim)}, \textit{Skyroad},
\textit{Interchange}, \textit{Combo}, \textit{Path to the bee},
\textit{Game One}, \textit{One Way Street}, \textit{Lasers},
\textit{On Thin Ice}, \textit{Robot Soccer}, \textit{Bridge-toggle
Maze}, \textit{Flipping}, \textit{Bit Match!}, \textit{Lime Richard},
\textit{Naomi's challenge 4 game winter}, \textit{abocalipc zumbi BR},
\textit{Puzz-Not}, \textit{Simple Block Pushing Game (David Skinner;
simple\_block\_pushing\_game\_by\_zacharybarbanell)},
\textit{Go Hunt $\Psi$\texttrademark{}![beta]}, \textit{Festive
Lights}, \textit{Rolling-block colour-zone mazes}, \textit{Build a
Snowball v2}, \textit{Remote Sokoban}, \textit{MR.Hooper},
\textit{Blind Ninja}, \textit{Sticky v0.7}, \textit{Green},
\textit{Cathartic Colours}, \textit{Cow Rancher}, \textit{buny
hoping}, \textit{Solitaire}, \textit{Mini Grapplenauts},
\textit{Telekin}, \textit{Carnival Shooter!}, \textit{Minecraft
v1.0.1}, \textit{My Game (Anonymous)}, \textit{Feed the Cat},
\textit{Test Golfer}, \textit{Maze (Mina Anderson)}, \textit{Move 3
Crates Next To Eachother}, \textit{Herding cats!}, \textit{J},
\textit{Schatkamer}, \textit{Arregla la vereda}, \textit{Test (Finn
Coffey)}, \textit{Simple Block Pushing Game (Stephen Lavelle;
simple\_block\_pushing\_game\_by\_meddlingcactus)}, \textit{CAT Making
Friends}, \textit{Tall Player}, \textit{My Game Is Better Than Your
Game}, \textit{A Very Good Puzzle}, \textit{Double Message},
\textit{Weeeee}, \textit{testi}, \textit{Amor en tiempos de
pandemia}, \textit{Space Invaders (e\_is\_cool $|$ edited by fz0718)},
\textit{Two Coats and Crates}, \textit{EA SPORTS' FIFA 2018 QATAR
WORLD CUP EDITION!}, \textit{SPACE INVADERS (Shaun Olafson [not my
OC])}, \textit{Platformer thing}, \textit{Underpuzzle},
\textit{Eventually This Is Gonna Be Zelda}, \textit{Endless
Practice}, \textit{Player Move, pause, Badfish move, pause},
\textit{title}, \textit{RGB}, \textit{PROTOTYPE}, \textit{Full Bloom},
\textit{Beat Game Test}, \textit{Cell Division}, \textit{test
(katelyn delta)}, \textit{?}, \textit{Drunkard walk}, \textit{Crossing
the Gap}, \textit{marbles}, \textit{Annoying Skull}, \textit{Colors},
\textit{PuzzleTracker}, \textit{card}, \textit{ICE WOLRD},
\textit{Test Teleportation}, \textit{Unlink}, \textit{PathFinding}.
}

\paragraph{\textsc{Heldout-30} and reported \textsc{Heldout-26}.}
The held-out evaluation set is disjoint from every training preset by
filename and by token-sequence hash. Drawn by stratified sampling
from the dedup pool to span low/mid/high rule counts. Main-body OOD
numbers report \textsc{Heldout-26}, which removes four games whose
tokenized rules exceed the conditional encoder's 657-token limit:
\textit{Angize}, \textit{Headless People Problems},
\textit{Break Out of the Mine}, and
\textit{Heroes of Sokoban - Ancient Japan}.
{\small
\textit{in the way}, \textit{Cyberpunk 2020}, \textit{My Nana Ate My
Kid Brother}, \textit{Ice Sliding}, \textit{Block game}, \textit{Kitten
Rescue}, \textit{Kyles chesse cuisine}, \textit{THE BEST 2D GAME EVER
CREATED}, \textit{Monophobic Multiban}, \textit{Charge The Bot},
\textit{Build a Snowman Challenges}, \textit{The Single Flame},
\textit{Pacman?}, \textit{Escape}, \textit{Megabot Invades!},
\textit{Link},
\textit{``\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_'' (\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_by\_ncrecc)},
\textit{Watchers' Ritual}, \textit{GDD301 Demo Game}, \textit{Surround
Yourself With Dogs}, \textit{Haberdashery}, \textit{Ghosts}, \textit{A
Snowball's Chance in Hell}, \textit{hedgehogger}, \textit{Cat
Adventure 2}, \textit{Hamiltwo}, \textit{Headless People Problems},
\textit{Break out of the Mine}, \textit{Heroes of Sokoban - Ancient
Japan}, \textit{Angize}.
}

\subsection{Architecture baselines: full per-preset table}
\label{app:baselines-full}

Table~\ref{tab:baselines-arch-full} expands the compact main-body
Table~\ref{tab:baselines-arch} with the random-AR and one-step (TF)
columns, plus per-preset parameter counts. Within each preset block,
rows are sorted by BFS-AR ascending and the per-block BFS winner /
fewest parameters are bolded. Param counts drift across blocks
because the input embedding and readout heads scale with each
preset's object/vocabulary footprint.

\begin{table}[t]
  \centering
  \caption{\textbf{Architecture baselines across three corpora ---
    full version.} Mean $\pm$ std over 3 seeds. All three error
    columns are per-cell, averaged across rollout steps and
    aggregated across games/levels; lower is better.}
  \label{tab:baselines-arch-full}
  \small
  \resizebox{\linewidth}{!}{%
  \input{figures/baselines/architectures}%
  }
\end{table}

\subsection{Per-game \textsc{Train-14} breakdown}
\label{app:per-game}

Figure~\ref{fig:per-game-heatmap} shows the per-game BFS final
cell-error on \textsc{Train-14}, averaged over each game's authored
levels. Three observations beyond the aggregate numbers in the
\textsc{Train-14} block of Table~\ref{tab:baselines-arch}:

\begin{itemize}
  \item \textit{Travelling salesman} is hardest for every architecture
    (NCA-perstep best at 38\%); the rule set encodes an NP-hard
    combinatorial problem that no single-step world model is
    expected to solve perfectly.
  \item Several games (\textit{neko puzzle}, \textit{Notsnake}, the
    basic-Sokoban demo, \textit{blocks.}) are fit to zero error by the
    NCA family but fail badly for U-Net (10--25\%) and ViT (1--33\%).
    The architecture gap is consistent across diverse rule sets, not
    an averaged-out artifact.
  \item \textit{ScriptCross} is a clean ViT-failure case (71\% error
    vs.\ 11--15\% for the rest), illustrating the structural
    mismatch we discussed in \S\ref{sec:results-scaling}.
\end{itemize}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../figures/baseline_comparison/scaling_14_per_game_heatmap.pdf}
  \caption{\textbf{Per-game BFS final cell-error on
    \textsc{Train-14}.} Rows are games (sorted by NCA-shared error,
    ascending); columns are architectures. Single seed at 20k updates.
    Both NCA variants beat the three non-NCA baselines on the vast
    majority of games; ViT is the only architecture with a
    catastrophic failure on a small game (\textit{ScriptCross}).}
  \label{fig:per-game-heatmap}
\end{figure}

\subsection{Reproducibility}
\label{app:reproducibility}

The codebase, dataset caches, training launchers, and result
aggregators are released alongside the paper. Each run is reproduced
end-to-end by re-running its launcher on the same hardware
(single 24~GB consumer GPU). The full experimental log accompanies
the released code.
% Internal notes (deanonymised paths kept as comments for the
% camera-ready / reproducibility supplement):
%   - Launchers: nca_wm/scripts/run_*.sh
%   - Per-game/per-arch synth grid: nca_wm/scripts/run_per_game_arch_synth_grid.sh
%     summarised via summarize_per_game_arch_grid.py --all_authored_as_heldout
%     and figures via nca_wm/scripts/make_within_game_synth_artifacts.py
%     (logs land in nca_wm/logs_per_game_arch_synth/)
%   - Running-state docs: nca_wm/SCALING_RESULTS.md, nca_wm/ARCHITECTURE_REPORT.md,
%     nca_wm/RUNNING_REPORT.md, project_synth_recipe.md

\section{Per-game $\times$ per-architecture grid}
\label{app:per-game-arch}

To check how architecture choice interacts with game type, we train
one model per (game, architecture-bucket) cell on five games covering
distinct mechanic classes---%
\textit{Microban} (single-step Sokoban-style box pushing: PuzzleScript's
basic push rule applies force to the cell directly in front of the
player, so a row of two boxes is \emph{not} chained-pushed by a single
action),
\textit{Heroes of Sokoban} (multi-bracket character swap and
projectile travel),
\textit{Bouncers} (non-axis-aligned looping projectile),
\textit{neko puzzle} (restricted-orientation movement), and
\textit{Travelling salesman} (multi-bracket all-pairs)---against
four architecture buckets at $T{=}8$:
(A) pool ON, shared ($n_r{=}T$);
(B) pool ON, per-step ($n_r{=}1$);
(C) no-pool $+$ input-skip, shared;
(D) no-pool $+$ input-skip, per-step.
Each cell is trained on the game's authored level 0 only with the
canonical recipe (\S\ref{sec:experiments}); we evaluate BFS rollout
cell-error on the training level (L0) and on held-out levels
(L1$+$, mean) separately. Training budget is 15k updates per cell,
extended to 50k for \textit{Travelling salesman} after observing
visible loss descent at 15k. Single seed. The same grid on the
basic-Sokoban demo (a smaller variant of the same Sokoban dynamics
as \textit{Microban}) is consistent with the \textit{Microban} row
and would double-count the same mechanic in the figure.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/heatmap_bfs.pdf}
  \caption{\textbf{Per-game $\times$ per-architecture BFS cell-error.}
    Rows: games. Columns: architecture buckets at $T{=}8$. Left panel:
    training-level (L0) cell-error. Right panel: held-out-levels (L1$+$
    mean) cell-error. Cells annotate the percentage. Bucket~D
    (no-pool $+$ input-skip $+$ per-step) wins held-out on five of six
    games. Bucket~A loses dramatically on \textit{Travelling salesman}
    L0 (45.1\%) despite identical train loss to bucket~D (which fits
    L0 to 0\%); see ``F3 sharpened'' below.}
  \label{fig:per-game-arch-heatmap}
\end{figure}

\paragraph{Train-loss-vs-rollout-error decoupling, sharpened.}
\label{app:F3-sharpened}
A model with the lowest per-step train loss is not the model with the
lowest autoregressive rollout error. Concretely, in the over-capacity
regime we observe per-step models with $5\times$ lower train loss but
$3$--$4\times$ higher 50-step rollout error: small but systematic
per-step errors compound destructively under autoregression. The
per-game grid pushes the gap to its loudest form yet.
On \textit{Travelling salesman} at 50k updates all four architecture
buckets converge to within $1\%$ of the same train loss
($1.70\times 10^{-3}$ to $1.72\times 10^{-3}$), yet L0 BFS cell-error
splits $0\% / 0\% / 26.9\% / 45.1\%$ across buckets D / C / B / A.
Bucket~A's L0 rollout is in fact \emph{worse} at 50k than at 15k
($45.1\%$ vs.\ $24.0\%$) despite a $68\times$ lower train loss---%
additional optimisation on a per-step minimum that compounds
destructively under autoregressive feedback. Buckets~C and D, which
replace global pool features with input-skip re-injection, converge to
the same train loss with a stable rollout. The pool-ON cells thus
appear to overfit per-step prediction in a way that does not survive
auto-regressive feedback.

\paragraph{Held-out gap and the synth-data prediction.}
Held-out cell-error (right panel of Fig.~\ref{fig:per-game-arch-heatmap})
remains substantial across every cell ($7.3\%$--$56.8\%$). This is the
\emph{within-game} OOD-levels axis---a complementary, lesser form of
generalization than the OOD-rules / latent-sampling / inverse-fit
results in Appendix~\ref{app:ood}. Prior single-game work strongly
predicts that synthetic levels close most of this gap: training
synth-only on the basic-Sokoban demo ($w{=}7$) reached $0\%$ cell-error
on 155 held-out \textit{Microban} / \textit{Microban I} authored
levels, and multi-grid synth on \textit{neko puzzle} closed a
$3$--$8\%$ gap to $0.3\%$ once the synth widths included the authored
aspect ratio. Both signals suggest the right-panel gap here is data
scarcity, not an architectural ceiling.
% Prior-work pointers (deanonymised; kept as comments):
%   - nca_wm/RUNNING_REPORT.md (single-game synth-only Sokoban run)
%   - nca_wm/SCALING_RESULTS.md (multi-grid nekopuzzle synth run)

\paragraph{Synth-only $\to$ all-authored generalization.}
\label{app:synth-only}
The within-game-generalization probe replaces each cell's authored
training set with $K{=}128$ synthetic levels generated at the
\emph{smallest-by-area} authored size of the target game and evaluates
on \emph{every} authored level (no L0 / heldout split: all authored
levels are out of distribution to the training set). We anchor on the
smallest authored size because (i) size-up generalization carries
synthetic small-grid training to bigger authored levels (Sokoban
$w{=}7 \to 0\%$ on 155 \textit{Microban}/\textit{Microban I}
authored levels in prior work) while size-down does not; (ii) using
a single size keeps the per-size data density at $K{=}128$, which
empirically matters: an early multi-grid variant
($K / n_{\text{sizes}}$ per size, $\approx 16$ levels per size for
\textit{Microban}) plateaued at $\approx 8\%$ BFS heldout while the same recipe
at fixed $K{=}128$ on a single small size reached $0.60\%$ BFS,
$0.02\%$ 1-step. We also tried an auto-bounding-box variant that
picks $\max W \times \max H$ for each game; it failed at $\approx 9\%$
BFS because many authored levels are much smaller than the bounding
box, and the synth distribution missed them. Smallest-by-area (rather
than per-axis $\min W \times \min H$) is used so the chosen size is
an actually-existing authored shape rather than a per-axis composite
(\textit{Microban}'s per-axis minimum is $6{\times}6$, but no authored
level is $6{\times}6$; smallest-by-area picks $6{\times}7$, which
appears in three authored levels).

The recipe trains on $K{=}128$ synthetic levels at the chosen size for
$30{,}000$ updates with the same four architecture buckets at $T{=}8$,
with synthetic-level generation in \emph{evolve} mode and a small
auxiliary token-decoder loss. To force the GA to spread the $K$-level
budget across the game's rule set rather than collapsing onto a single
easy mechanic, we enable rule-coverage-driven evolution: per-step
rule-firing telemetry biases the fitness to
$\text{BFS-iterations} + 100 \cdot |\text{unique rules fired}|$, and
at the end of evolution we greedily pick the $K$ levels that maximize
the \emph{union} of rules covered (rather than top-$K$ by individual
fitness). For small-rule-set games (e.g.\ Sokoban-class) this is a
no-op; for multi-bracket games (\textit{Heroes of Sokoban},
\textit{Travelling salesman}) it materially changes which $K$ levels enter
training. We also report the iterations-only baseline (no coverage
weighting, no union-greedy selection) as a side-by-side ablation
column in Table~\ref{tab:within-game-synth}. The pre-registered
prediction follows in \S\ref{app:within-game-prediction}. If pure
synth fails on a game we fall back to seeding from one or a handful
of authored levels alongside synth---a lesser form of within-game
generalization that still tests few-shot extrapolation.
% Internal pointers (paths kept as comments for the reproducibility
% supplement; they reference deanonymised tooling, so they should not
% appear in the submission body):
%   - Launcher: nca_wm/scripts/run_per_game_arch_synth_grid.sh
%   - Summariser: nca_wm/scripts/summarize_per_game_arch_grid.py
%       --all_authored_as_heldout
%   - Logs land in nca_wm/logs_per_game_arch_synth/
%   - Validated post-1fa557d on Microban, bucket D, 30k updates;
%     see project_synth_recipe.md for full validation history.
%   - Recipe flags:
%     --synthetic_levels 128 --synthetic_w <min_W> --synthetic_h <min_H>
%     --synthetic_fallback_dynamics --synthetic_no_a_count_max 5
%     --synthetic_mode evolve --token_decoder_loss_weight 0.1
%     --synthetic_track_rules_fired --synthetic_rule_coverage_weight 100
%     --synthetic_coverage_select_topk

% \papertodo{Fill the synth-only column once the sweep completes:
% regenerate the per-game/per-architecture heatmap as a 4-panel figure
% (L0 authored vs.\ heldout authored vs.\ synth-only all-authored, with
% synth-train-loss as the bonus panel) and update
% Table~\ref{tab:within-game-synth}.}

% \papertodo{If synth-only beats authored-L0-only on \textit{Microban}
% held-out (Sokoban prior says yes), \textit{Heroes of Sokoban} (multi-bracket
% char-swap; less certain), and \textit{Bouncers} (looping projectile; uncertain),
% that closes the within-game OOD-levels claim across three distinct
% mechanic classes. \textit{Travelling salesman} is the canonical hard
% case: if the synth-only run reproduces the F3 sharpening (same train
% loss, $40{+}$pp rollout split between buckets) it is the cleanest
% demonstration of the per-step-vs-AR decoupling we report, since no
% authored memorization remains to confound the comparison.}

% \papertodo{\textbf{Extensions.} (i) Add a depth axis $T \in \{4, 16, 32\}$
% to test whether bucket A's pool-driven AR-rollout failure shrinks at
% larger $T$; (ii) add games covering gravity-chain (\textit{MazezaM}),
% counter / state aggregation (\textit{Modality}), and length-state
% (\textit{Lime Rick}) mechanics; (iii) re-run the existing five cells
% with three seeds, since prior work (F8, \S\ref{app:ceiling})
% showed per-seed variance can dwarf recipe deltas on similar
% single-game multi-grid tasks.}

\section{Within-game generalization from synthetic levels}
\label{app:within-game}

The fidelity numbers in Table~\ref{tab:indist-scaling} measure cell-error
on the same authored levels the model trained on. To probe whether
the rule-conditioned NCA learns rule structure rather than per-state
outcomes within a single game, we strip every authored level out of
the training set and substitute $K{=}64$ purely-synthetic levels per
game generated by an evolutionary search at the game's authored
max-dim. Evaluation is unchanged: BFS / A* / random-action rollouts
on \emph{every} authored level. Five games span distinct mechanic
classes (\textit{Microban}, \textit{Heroes of Sokoban},
\textit{Bouncers}, \textit{neko puzzle},
\textit{Travelling salesman}); each game is trained four times to
cover the architecture buckets in Appendix~\ref{app:per-game-arch}
(pool $\times$ weight-sharing at $T{=}8$). The recipe and launcher
are documented in \S\ref{app:synth-only}.

Pure-synthetic supervision is the strongest available within-game
OOD-levels probe: every authored level is genuinely held-out from
training; the only signal the model has about the rule set is the
tokenized rule text plus rollouts on synthesized grids. A model that
has memorized authored states cannot succeed here.

\begin{table}[t]
  \centering
  \caption{\textbf{Synth-only $\to$ all-authored generalization.}
    Each cell is the mean BFS rollout cell-error across every authored
    level of the game, after training on $K{=}128$ purely-synthetic
    levels at the smallest-by-area authored size of the game.
    No authored level enters training. Best architecture bucket per
    game is bolded. Cells marked ``—'' are not reported here;
    the displayed \textit{Microban}-D number is the anchor result
    described below.}
  % Auto-regenerated from nca_wm/logs_per_game_arch_synth/ by
  % nca_wm/scripts/make_within_game_synth_artifacts.py
  \label{tab:within-game-synth}
  \small
  \input{figures/per_game_arch_synth/within_game_synth_table.tex}
\end{table}

\paragraph{Anchor result.}
A pilot run of the validated recipe (\S\ref{app:synth-only}) on
\textit{Microban}, bucket D ($T{=}8$, no-pool $+$ input-skip,
per-step), at the smallest-by-area authored size, $K{=}128$,
$30{,}000$ updates produces the rich-metric numbers in
Table~\ref{tab:within-game-synth-anchor}. Five of the ten authored
levels land at $0\%$ BFS exactly. The $0.02\%$ teacher-forced figure
is in fact $5\times$ \emph{lower} than the authored-only baseline
of $0.11\%$ at $10{,}000$ updates (Sokoban authored block,
Table~\ref{tab:baselines-arch}), demonstrating that synth-only
training with a coverage-driven GA produces a \emph{more accurate}
per-step rule predictor than authored-only training, with the BFS
rollout still close to authored (within $5\times$). This is the
within-game OOD-levels evidence the section advertises: the
rule-conditioned NCA does learn rule structure rather than
per-authored-state outcomes within a single game.

\begin{table}[t]
  \centering
  \caption{\textbf{Anchor run for the synth-only recipe.} Mean per-cell
    error across all 10 authored \textit{Microban} levels (no level
    enters training).}
  % Auto-regenerated from
  %   nca_wm/logs_per_game_arch_synth/Microban__D_d8_w7_k128_30k
  % by nca_wm/scripts/make_within_game_synth_artifacts.py
  \label{tab:within-game-synth-anchor}
  \small
  \input{figures/per_game_arch_synth/anchor_microban_d.tex}
\end{table}

\paragraph{Pre-registered prediction for the remaining cells.}
\label{app:within-game-prediction}
Prior single-game evidence is sharp on at least the Sokoban-class
subset (cf.\ the Sokoban $w{=}7 \to 0\%$ on 155
\textit{Microban}/\textit{Microban I} levels prior;
\textit{neko puzzle} multi-grid closing a $3$--$8\%$ authored gap to
$0.3\%$). We expect (i) Sokoban-class games (\textit{Microban}) to
land at $\leq 1\%$ BFS across all four buckets, replicating the
\textit{Microban}-D anchor; (ii) \textit{Bouncers} and
\textit{Heroes of Sokoban} to show non-trivial transfer with the
architecture gap (D vs.\ A) at least as large as in the authored
case; (iii) \textit{Travelling salesman} to fail under pure synth
supervision at $5{\times}5$ and require the authored-seeded fallback,
since the all-pairs combinatorial structure of larger TSP grids is
not present in the small-grid synth distribution.

\paragraph{Decoupling reprise without memorization.}
On \textit{Travelling salesman}, the per-game grid in
Appendix~\ref{app:per-game-arch} already showed all four
architecture buckets converging to within $1\%$ of the same
synthetic-style train loss while their L0 BFS cell-error split
$0\% / 0\% / 26.9\% / 45.1\%$. The synth-only setup pushes that
test further: with no authored memorization available, any
train-loss parity across buckets is forced to come from learning
rule structure, so an even larger AR-rollout split would be the
cleanest available signal that pool-driven over-fitting on per-step
prediction destabilizes auto-regressive feedback.

\section{Architecture findings}
\label{app:arch-findings}

The architecture comparison in \S\ref{sec:results-baselines} reports a
single number per architecture; this appendix expands the underlying
architectural findings. We label the findings F1--F3 to align with
the architecture report released alongside the code.

\paragraph{(F1) Global pool is load-bearing; axis-cummax targets a
narrower rule class.}
A bare $3{\times}3$ convolution can only propagate one cell of context
per NCA step. Several PuzzleScript rule patterns reference cells
arbitrarily far apart along an axis or anywhere on the level
(\texttt{[X | ... | Y]}, \texttt{[X][Y]}). Without pool features the
NCA needs $\Omega(\max(H,W))$ steps just to surface non-local
information. The per-feature ablation in
Table~\ref{tab:ablation-pool} isolates which pool flavour does the
work: removing global-pool alone is as costly as removing all three
($+145\%$ relative BFS error), while removing axis-cummax has no
measurable effect on the basic-Sokoban demo (its target patterns---%
axis-aligned \texttt{[X | ... | Y]} matches---do not appear in
that game's rule set). On a 19-game gallery preset and a controlled
\textit{Collapse} no-pool ablation, turning pool off costs both
convergence speed and final loss, with the no-pool model collapsing
into the identity-prediction minimum even at $4{\times}$ the depth.

\paragraph{(F2) Shared NCA-body weights are the right inductive bias.}
The PuzzleScript engine has two natural levels of iteration: an
inner ordered list of rules applied once each per tick, and an outer
\texttt{again} loop that re-runs the rule list until the state stops
changing. We factor $T = n_\ell \cdot n_r$ accordingly. On a
single-game depth sweep ($T \in \{2, 4, 8, 16\}$), shared $T{=}8$
reaches $5.5\times$ lower train loss with $7\times$ fewer parameters
than the per-step body at the same depth; shared $T{=}2$ at
$14\times$ fewer parameters than the deepest per-step variant
matches it on oracle-action rollouts. Hierarchical $n_r > 1$ sits
between these extremes (\S\ref{app:hierarchy}).

\paragraph{(F3) Train loss decouples from autoregressive rollout error.}
\label{app:F3}
A model with the lowest train (per-step) loss is not the model with
the lowest autoregressive rollout error. Concretely, in the
over-capacity regime we observe per-step models with $5\times$ lower
train loss but $3$--$4\times$ higher 50-step rollout error. The
mechanism is small but systematic per-step errors compounding
destructively under autoregression. Reporting only best-loss hides
the rollout-drift cost of going deeper or wider.
Appendix~\ref{app:per-game-arch} reports a sharpened version of this
finding on a per-game $\times$ per-architecture grid: on
\textit{Travelling salesman} all four buckets converge to within
$1\%$ of the same train loss but split
$0\% / 0\% / 26.9\% / 45.1\%$ on L0 BFS cell-error.

\section{Hierarchical weight sharing}
\label{app:hierarchy}

% \papertodo{Add hierarchy figure/table: $T{=}16$ varying
% $n_r \in \{1, 2, 4, 8, 16\}$ on \textit{Microban} multi-grid synth
% and on the multi-game \textsc{Train-14} preset, reporting
% change-error and parameter count.}

\section{Limitations and future work: gated local updates}
\label{app:gated-updates}

Our NCA body applies an ungated convolutional update at every iteration: each step replaces the hidden cell vector with the output of a small conv $+$ activation stack (with optional input-skip re-injection).
By contrast, the closest prior local-update grid-engine model~\cite{bamford2020neural} uses a 2D Neural GPU~\cite{kaiser2016neuralgpu}, whose core is a convolutional GRU (CGRU) with explicit update and reset gates: the new state is a convex combination $u \odot s_{i-1} + (1-u) \odot c_i$ of the previous state and a candidate update.
Two concrete directions are worth exploring.
First, plugging a CGRU-style gate into our shared-body NCA would let the model express ``hold this cell unchanged'' as a learnable, content-dependent identity rather than relying on the residual recurrence we currently use; this is the same mechanism that lets a Neural GPU run for many iterations past convergence without drifting, and it interacts naturally with our adaptive-halting experiments (Appendix~\ref{app:halting}), where convergence-ST already exploits self-arrest at inference time.
Second, gating could change the train-loss-vs-rollout-error decoupling we report in F3 (Appendix~\ref{app:F3}): per-step models that overfit per-step prediction at the cost of autoregressive stability might be exactly the regime where a learned ``do nothing'' gate dampens compounding error.
Whether the inductive bias matches the PuzzleScript engine's outer-loop semantics as well as our ungated body does is an open empirical question---the engine's \texttt{again} loop also expresses ``re-run rules until the state stops changing,'' so a gating mechanism that learns a content-dependent halt is in principle a closer match to the target dynamics than our fixed-depth body.
We leave a controlled CGRU-vs-ungated comparison at matched parameter count and matched depth to future work.

\section{Additional related work}
\label{app:related}

\paragraph{Adaptive computation.}
PonderNet~\cite{banino2021pondernet} learns a per-input halting
distribution over a recurrent module, with a KL prior over the number
of iterations. Adaptive Computation Time~\cite{graves2016act} and
Universal Transformers~\cite{dehghani2018universal} are predecessors.
Our adaptive-halt experiments
(Appendix~\ref{app:halting}) adapt PonderNet to NCA world models
with a shared body, so that ``halt at step $k$'' coherently asks the
same rule set to converge in $k$ iterations rather than selecting
between $k$ different models.

\section{Adaptive halting}
\label{app:halting}

We compare four halting modes and a fixed-depth baseline on a custom
$1$-rule \textsc{varislide} game in which a single \texttt{again}-driven
slide rule pushes the player up to $d \in \{1, 2, 3, 4, 6, 8, 12, 16\}$
cells per action across grid widths $\{6, 8, 10, 12, 16\}$. Modes:
\emph{ponder} (PonderNet ponder loss with learned halt head + KL
prior), \emph{uniform} (per-step loss equally weighted across all
$T$ steps), \emph{argmax-ST} (straight-through estimator on
$\arg\max_k p_k$), and \emph{convergence-ST} (halt at inference when
consecutive predictions stop changing, no learned head).

% \papertodo{Add halting figure: per-step error trajectory under each
% mode plus per-instance halt distribution as a function of slide
% distance.}

Across these modes we observe two phenomena. First, on tasks where
the dynamics fit cleanly in $\approx 1$--$2$ NCA steps (small-grid
varislide, \textsc{collapse}), all halting modes converge near the
dynamics-floor within their effective depth and drift past it;
convergence-ST recovers near-oracle performance via inference-time
stopping. Second, on the multi-grid synthetic regime where
memorization is unavailable, \emph{none} of the halting modes
reliably learn the underlying rule (\S\ref{app:ceiling}); the halt
head at best degenerates to halt-at-$k{=}1$ + identity prediction.

\section{Multi-grid synthetic data ceiling}
\label{app:ceiling}

When we move from a single authored level to the multi-grid synthetic
data regime ($\{6,8,10,12,16\}$ widths $\times$ 64 levels each),
train change-error climbs by $1000\times$
($5 \times 10^{-5} \to 0.05$--$0.10$), confirming that the model can
no longer 1-shot-memorize per-state outcomes. The model attempts to
learn the rule but plateaus: 50k updates at $d_h{=}256$ ($5\times$
more updates, $2\times$ wider) does not move the needle. Multi-seed
verification at $T \in \{8, 16, 32, 64\}$ shows per-seed argmax
variance dwarfs the recipe signal and best-loss is essentially
identical across all configurations. We discuss candidate fixes in
\S\ref{sec:discussion}.

\section{In-distribution dilution as the training corpus grows}
\label{app:dilution}

This appendix accompanies Figure~\ref{fig:intersection-slope} in the
main body, which plots the headline 1-step (TF) trajectory of the
14 games shared by every training preset. We report the per-game
table here, plus the per-preset aggregate over the (changing) full
training set for completeness.

\subsection{Per-preset aggregate over the full training set}
\label{app:indist-scaling-aggregate}

For reference,
Table~\ref{tab:indist-scaling} pairs each rule-conditional checkpoint
(\textsc{Train-14}, \textsc{Train-59}, \textsc{Train-199}) with the
unconditional baseline at the same preset, averaging across all
training games at that scale. Because each preset trains on a
different game set, the per-preset numbers are not directly
comparable across rows---harder games entering at \textsc{Train-59}
and \textsc{Train-199} drive part of the climb, the per-game
dilution captured by Figure~\ref{fig:intersection-slope} drives the
rest.

\begin{table}[t]
  \centering
  \caption{\textbf{In-distribution per-cell rollout error as the
    training corpus scales, with the unconditional baseline at each
    scale.} Mean / median across all training games at the given
    preset (each game's number is the per-rollout-step mean across
    all authored levels).}
  \label{tab:indist-scaling}
  \small
  \input{figures/indist_scaling/table}
\end{table}

\subsection{Per-game intersection over 14 shared games}

Restricting evaluation to the 14 games shared by every preset
(Table~\ref{tab:indist-dilution}) holds the games fixed and reads
the per-game effect of corpus size directly. All six runs share
the same architecture, hidden width $n_h{=}256$, training budget
(150k updates), batch size, balanced sampling, and recipe; only
the training corpus differs.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.85\linewidth]{figures/indist_intersection/intersection_heatmap.pdf}
  \caption{\textbf{Per-game 1-step (TF) cell-error (\%) on the 14
    intersection games, log-color scale.} Rows sorted by maximum
    cell value, descending; columns grouped by preset (ascending
    corpus size) with conditional / unconditional paired at each
    scale. The catastrophic unconditional failures
    (\textsc{nekopuzzle}@\textsc{Train-59}, $24.4\%$;
    \textsc{notsnake}@\textsc{Train-199}, $13.1\%$) drive the
    aggregate mean in Figure~\ref{fig:intersection-slope}'s left
    panel; the rule-conditional columns at every scale stay an
    order of magnitude lower across the board.}
  \label{fig:intersection-heatmap}
\end{figure}

\begin{table}[t]
  \centering
  \caption{\textbf{Per-game 1-step (teacher-forced) cell-error (\%) on
    the 14 games shared by every training corpus.} Mean across each
    game's authored levels; lower is better. The bottom two rows are
    mean and median across the 14 intersection games. Columns are
    grouped by training preset (ascending corpus size) with the
    conditional and unconditional model paired at each scale.}
  \label{tab:indist-dilution}
  \small
  \input{figures/indist_intersection/table}
\end{table}

Two effects coexist (Table~\ref{tab:indist-dilution},
Figure~\ref{fig:intersection-slope}). First, both
conditional and unconditional models degrade on the shared 14 games as
the corpus widens; conditional 1-step (TF) mean grows
$0.06\% \to 0.27\% \to 0.37\%$ across \textsc{Train-14},
\textsc{Train-59}, \textsc{Train-199}, and the median grows
$0.006\% \to 0.021\% \to 0.157\%$. At fixed capacity, adding more
training games costs each shared game a roughly constant share of
model capacity / effective updates---data dilution rather than a
failure of the architecture. Second, rule-conditioning slows that
dilution by roughly an order of magnitude. From \textsc{Train-14} to
\textsc{Train-59}, conditional 1-step (TF) mean grows $\approx 5\times$
($0.057\% \to 0.266\%$) while unconditional mean grows $\approx 25\times$
($0.087\% \to 2.20\%$); on individual games the gap can be wider
(\textit{neko puzzle}'s 1-step (TF) goes $0.000 \to 24.4\%$ unconditional
versus $0.000 \to 0.000\%$ conditional). The encoder is doing real
work amortizing shared dynamics across games, just not enough to
cancel dilution at this capacity.

This decouples two things the main-body Table~\ref{tab:indist-scaling}
otherwise blurs: rule-conditioning carries its weight on shared
games (it slows in-distribution dilution and helps OOD;
\S\ref{sec:results-encoder-ablation}), while $n_h{=}256$ caps how
many games can be jointly mastered without per-game accuracy
degrading. A capacity sweep at \textsc{Train-199} would say which
of the two limits binds first; we leave that to future work.

\section{Generalization to unseen games}
\label{app:ood}

The 30-game stratified held-out set is disjoint from every training
preset by name and by token-sequence hash; main OOD rollout tables
report the 26 non-truncated games whose tokenized rules fit the
conditional encoder. We probe generalization along three axes:
held-out latent geometry, symbolic decoder
reconstruction and sampling, and frozen-NCA inverse fitting from
observed transitions.

\subsection{Per-cell rollout error breakdown on \textsc{Heldout-26}}
\label{app:cond-vs-uncond}

Table~\ref{tab:cond-vs-uncond-match} accompanies
Figure~\ref{fig:intersection-slope-ood} with the four error regimes
broken out per training preset, including BFS / A* oracle-action
rollout where those refreshes have completed and the wins-vs-identity
count summarized in
\S\ref{sec:results-encoder-ablation}.

\begin{table}[t]
  \centering
  \caption{\textbf{Out-of-distribution per-cell rollout error on
    \textsc{Heldout-26}.} \textsc{Heldout-26} drops the four
    \textsc{Heldout-30} games whose tokenized rules exceed the
    conditional encoder's 657-token limit, avoiding rule truncation at
    evaluation time. Mean / median are across the 26 retained games.
    The four error columns mirror Table~\ref{tab:indist-scaling};
    BFS / A* cells are shown when that refresh has completed for a
    row. The final column counts games where the model's 1-step (TF)
    mean error is lower than identity's; identity predicts
    $\hat{s}_{t+1}{=}s_t$ and lower-bounds the per-step state-change
    rate.}
  \label{tab:cond-vs-uncond-match}
  \small
  \input{figures/cond_vs_uncond_match/match_table}
\end{table}

\subsection{Held-out latent geometry as we scale games}
\label{app:scaling-curve}

For each held-out game we encode its tokens through the trained
encoder, compute cosine distance to every training game's encoded
slot, and report the smallest of those distances---the distance
to the held-out game's \emph{nearest training-game neighbour} in
slot space. We use the flattened
$K \cdot d_{\text{slot}} = 1024$-dim slot vector, and the median
across the 21 of 30 held-outs whose tokens fit within the smallest
training preset's maximum sequence length.

We treat this as a \emph{diagnostic}, not a quality metric. A
distance of zero is collapse, not perfect transfer (the held-out
game has been mapped onto an existing training point). A large
distance is off-manifold, also bad. The number is also naturally
expected to shrink as the training preset grows---with more
games the chance that some training game lies near any given
held-out target rises mechanically, independent of representation
quality. Predictive OoD performance---per-cell error of the dynamics model
on the held-out set's authored-level transitions, against an
identity baseline---is the load-bearing claim for unseen-game
generalization; the geometry numbers below are reported only for
context.

\begin{table}[t]
  \centering
  \caption{\textbf{Held-out latent geometry as we scale training
    games.} Cosine distance, in flattened-slot space, from each
    held-out game's encoded slot to its nearest training-game
    neighbour. All runs use the rule-attention NCA body, $K{=}16$
    slots, $d_{\text{slot}}{=}64$, $T{=}8$ NCA steps, batch 32, 150k
    Adam updates. The held-out set is the same 30-game stratified
    sample for every row.}
  \label{tab:scaling-curve}
  \small
  \begin{tabular}{l r l r}
    \toprule
    preset & $n_{\text{games}}$ & decoder & median distance to NN train game \\
    \midrule
    \textsc{Train-14}             & 14 & off & $0.077$ \\
    \textsc{Train-94}             & 94 & off & $0.215$ \\
    \textsc{Train-94}             & 94 & on  & $\mathbf{0.153}$ \\
    \bottomrule
  \end{tabular}
\end{table}

At 94 training games the encoder-only median is larger than at
$14$ ($0.215$ vs.\ $0.077$); the 14-game number is partly a
consequence of the held-out games being homogeneous push-puzzles
that sit close to any of several push-puzzle training games at
that scale. The 94-game decoder-on row drops to $0.153$, but
because of the diagnostic caveats above we do not read this as
evidence the decoder objective improves transfer; it could equally
indicate decoder-induced collapse onto training neighbours.
Whether those neighbours are \emph{mechanically} related to the
held-out target is a separate question---two example matches in
\S\ref{app:inverse-fit} suggest the geometry is not pure
collapse, but we have not enumerated the relation across the full
held-out set.

\subsection{Joint symbolic-decoder objective is free for in-distribution fidelity}
\label{app:joint-decoder}

\paragraph{Default training setup.}
The main-body Conditioning interface paragraph
(\S\ref{sec:method}) writes the training loss as
$\mathcal{L} = \mathcal{L}_{\text{state}} + \alpha \mathcal{L}_{\text{win}}$.
The actual training objective adds a third
auxiliary term $\beta \mathcal{L}_{\text{tok}}$ whose role is purely
to shape the slot space: the encoded slots $z$ are fed to a causal
token decoder $\mathrm{Dec}_\psi$ that is trained under teacher
forcing to reconstruct the original token sequence (decoder
architecture and reconstruction / sampling results in
Appendix~\ref{app:autoencoder}). All conditional runs reported in
the main body train with $\beta = 1.0$ by default; we never observed
the auxiliary loss to harm world-model fidelity, and switching it on
is what makes the slot-space probes in
Appendices~\ref{app:autoencoder} and \ref{app:inverse-fit} possible
(the decoder is what turns a slot back into program text).

A natural concern when adding this auxiliary loss is whether it
costs anything on the world-model's primary job---predicting the
next state. Table~\ref{tab:fidelity} compares two checkpoints that
share architecture, training preset (\textsc{Train-94}: 94 deduped
Gallery games), and update count, and differ only in whether the
decoder loss is on. Per-cell rollout error ties within $0.5$pp on
every regime, so the in-distribution dynamics-fidelity claim is
robust to the auxiliary loss.

\begin{table}[t]
  \centering
  \caption{\textbf{Joint decoder objective is essentially free for
    in-distribution fidelity.} Per-cell rollout error on the 94-game
    \textsc{Train-94} preset for the encoder-only ($\beta = 0$) and
    joint-decoder ($\beta = 1.0$) checkpoints. Mean / median across
    all 94 games (each game's number is the per-rollout-step mean
    across all authored levels, 50 steps for autoregressive regimes /
    level-length for BFS / A*). Every regime ties within $0.5$pp.}
  \label{tab:fidelity}
  \small
  \input{figures/fidelity/table}
\end{table}

% TODO: re-run the encoder-only / joint-decoder pair with the same
% preset and update count under the canonical recipe, so this
% comparison sits on the same footing as the cond_match runs that
% populate Table~\ref{tab:indist-scaling} and
% Table~\ref{tab:cond-vs-uncond-match}.

\subsection{Symbolic decoder: reconstruction and sampling}
\label{app:autoencoder}

\paragraph{Decoder architecture.}
$\mathrm{Dec}_\psi$ is a four-layer causal Transformer over the same
$\approx 140$-symbol vocabulary used by the encoder. Each token position
self-attends causally to prior positions and cross-attends to the
$K$ slots at every layer. We train under teacher forcing: the input
sequence is the right-shifted target tokens with a learned BOS, and
the per-position loss is masked cross-entropy against the next
target token. Object names and sprite-RGB tokens are vocab-extended
on a per-game basis; the decoder treats them as ordinary tokens and
the detokenizer assigns generic names $\mathrm{Obj}_i$ on round-trip
to .puzzlescript source. Validity checks (parser-loadable +
JS-engine-compilable) are reported as separate metrics below, since
syntactic well-formedness and engine acceptance both diverge from
exact-token reconstruction in the OOD-sample regime.

The joint encoder + NCA + slot-token decoder is trained on
\textsc{Train-94} for 150k updates with $\beta = 1.0$ on the decoder
loss. The decoder converges to per-token cross-entropy
$\approx 2 \times 10^{-9}$ within the first 5k updates and holds
there throughout, adding 1.18M parameters on top of the 9.89M
world-model parameters and roughly 20\% to per-step wall-clock.

\paragraph{Token reconstruction accuracy} (per-position match rate
between the decoder's argmax prediction and the target token under
teacher forcing on training games): perfect reconstruction on
$76/94$ training games and a median per-position match of $1.000$
(mean $0.906$, min $0.276$). The slot bottleneck retains the
information needed to recover the full symbolic specification on the
majority of the corpus; the imperfect mean is dominated by a small
tail of long, sprite-rich games that exhaust the decoder's
positional budget.

\paragraph{Engine-load success rate} (fraction of decoded token
sequences that, after detokenization to PuzzleScript source, parse
and compile via the JS engine): $15/25$ engine-loadable PuzzleScript
programs ($60\%$) from greedy autoregressive decoding from random
Gaussian-fit slots; $9/9$ engine-loadable programs along a 9-point
linear interpolation between two endpoint training games
(\textit{Ebony \& Ivory} $\rightarrow$ \textit{BROKEN Naive Extended
Bodies}; $100\%$). The generations contain plausible push rules of
the form
\verb"[ > Obj2 | Obj4 ] -> [ > Obj2 | > Obj4 ]",
\texttt{late}-prefixed rules, and per-object $5{\times}5$ pixel
sprites with palettes consistent across the legend, the collision
layers, and the rule body.

\subsection{Inverse-fitting unobserved games from transitions}
\label{app:inverse-fit}

Because the dynamics model is conditioned on a continuous game
representation, we can freeze the NCA parameters and infer an
unobserved game embedding from transition data. Concretely, we
freeze the world model and decoder $(\theta, \psi)$ and treat
$z \in \mathbb{R}^{K \times d_{\text{slot}}}$ as the only free
variable: the encoder $\mathrm{Enc}_\phi$ is bypassed entirely and
the optimised $z$ enters the world model through a slot-override
path. We initialise $z$ from a small Gaussian around the empirical
training-slot mean and optimise the per-cell next-state BCE used
during training, summed over the held-out target's observed
transitions, for 2{,}000 Adam steps at $\text{lr} = 5 \times 10^{-3}$
with batch size 32. Targets are drawn from the deduped pool and
verified disjoint from training by name and by token-hash. After
fitting, we report the cosine distance from the recovered $z$ to
its nearest training-game slot---``nearest train'' in
Table~\ref{tab:inverse-fit} means the closest training-game slot in
the flattened-slot space, not a comparison against a held-out
reference encoding of the target.

\begin{table}[t]
  \centering
  \caption{\textbf{Inverse-fit recovery from OOD transitions.}
    Final per-cell BCE on the observed transitions of three
    held-out games, and the cosine distance from the optimised slot
    to its nearest training-game neighbour (with that neighbour's
    name).}
  \label{tab:inverse-fit}
  \small
  \begin{tabular}{l c c l}
    \toprule
    held-out target & final BCE & dist.\ to NN train & nearest train game \\
    \midrule
    \textit{Monophobic Multiban} & $\mathbf{8.4 \times 10^{-4}}$ & $0.389$ & \textit{Run em' over} \\
    \textit{Cat Adventure 2}     & $\mathbf{8.4 \times 10^{-4}}$ & $0.394$ & \textit{neko puzzle (lexaloffle)} \\
    \textit{in the way}          & $7.2 \times 10^{-3}$         & $0.477$ & \textit{Run em' over} \\
    \bottomrule
  \end{tabular}
\end{table}

The optimisation drives BCE down two-to-three orders of magnitude on
every target, so the frozen NCA can fit OoD dynamics through the
latent variable. The reported BCE is on the same transition tape
the slot was optimised against, however, so it measures the
expressive capacity of $(\theta, \psi)$ plus the optimisation
landscape, not held-out generalisation; an honest upgrade would
hold out a fraction of the tape, fit on the rest, and report
test-tape BCE.

On two of three targets the recovered slot's nearest training-game
neighbour matches a plausible mechanical sibling:
\textit{Cat Adventure 2} (a push-puzzle) lands closest to
\textit{neko puzzle} (also a push-puzzle), and
\textit{Monophobic Multiban} (a multi-character chase mechanic)
lands closest to \textit{Run em' over} (also multi-character /
chase). We do not yet have a systematic enumeration of how often
the nearest neighbour is mechanically related across the full
held-out set; the two matches above are illustrative.
